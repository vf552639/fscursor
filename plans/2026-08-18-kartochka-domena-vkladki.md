# Карточка домена: перенос вкладочного дизайна

## Context

Дизайнер прислал хендофф `Domain Modal.dc.html` + README (распакован в скратчпад,
исходник — `/Users/andrey/Documents/Редизайн с вкладками для страницы.zip`): модалка
домена шириной 920px, разложенная по пяти вкладкам **Overview / Server / Logs /
Template / Custom**, с единым паттерном карточки (шапка-полоска `#eef2f7` + тело),
пилюлями статусов и инлайн-правкой срока в шапке.

Сегодняшняя `DomainDetailModal` — 760px, **без вкладок**, и это было решением
(комментарий в файле, строки 74-93): вкладки `db/ssl/nginx` удалили планом
`plans/2026-08-11-domains-chistka-sloya-ispolneniya.md`, потому что их действия били в
несуществующие роуты. Макет предлагает другие пять вкладок, и две из них (Overview,
Server) — это ровно то, что карточка уже показывает после плана
`plans/2026-08-17-kartochka-domena-svyazi-i-registrar.md` (ряд связей Registrar →
Cloudflare → Server, SSL, Nameservers, Server state с правилом расхождений).

**Задача:** перенести визуальную систему макета и вкладочный каркас, не потеряв
семантику, которую карточка заработала. Разведка показала, что макет рисует Overview и
Server **проще**, чем текущая карточка: в нём нет строк расхождения «при развёртывании:
X», нет легенды `noSnapshot`, нет перечня FTP-аккаунтов, нет `RevealSecret` для пароля,
нет `SSL at provision` и `Last error`. Дизайнер про них не знал. Дословный перенос стёр
бы работу плана от 17.08, поэтому правило переноса такое: **макет даёт вёрстку и
раскладку по вкладкам, существующая семантика переезжает внутрь неё целиком.**

### Решения, принятые в брейншторме (2026-08-18)

1. **Тема — светлая, как в макете.** Хексы инлайном, как везде в приложении. Токены и
   тёмная тема из `design-brief.md` остаются отдельной будущей работой; эта карточка
   тёмного варианта у дизайнера не имеет.
2. **Только вёрстка сейчас, данные потом.** Logs / Template / Custom / Backups — новые
   функции, за которыми нет ни моделей, ни SSH-команд. Они получают **честные пустые
   состояния** по образцу DMCA из самого макета, а не демо-данные с мёртвыми кнопками
   («Run», «Save», 20 выдуманных строк лога). Это прямо следует из принципа 6 CLAUDE.md.
3. **Язык — английский, как в макете.** Новые строки пишем по-английски; двуязычие
   (долг №9 плана от 17.08) при этом не лечим — русские кнопки `Проверить на сервере`,
   `Задать пароль` остаются как есть, отдельным долгом.
4. **Срок истечения правится из шапки** — бэкенд готов (`PUT /domains/{id}`,
   `DomainUpdate.expiry_date`), мутация `useUpdateDomain` уже используется в карточке
   для регистратора.

### Что за макетом есть в приложении (разведка)

| Вкладка  | Данные                                                                                                                                              |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| Overview | **есть почти всё** — registrar/CF/server, `sslState`, RDAP-делегирование, NS                                                                        |
| Server   | **есть, кроме бэкапов** — FTP, Site (owner/PHP/БД); бэкапов нет нигде: ни модели, ни поля, ни команды FastPanel в инвентаре `docs/FASTPANEL_CLI.md` |
| Logs     | **есть только метаданные** — `fp_facts.logs[] = {path, exists, size_bytes}`; чтения содержимого нет ни в Rust, ни на бэкенде                        |
| Template | **нет вовсе** — ни модели, ни поля, ни свободного текста у `Domain`                                                                                 |
| Custom   | **нет** — `nginx_presets` есть в БД и в Rust (`render_nginx_snippet`, 4 флага), но ни роутов, ни UI, ни вызова                                      |

## Осознанные отступления от макета

Каждое — потому что макет не знал контекста, и переносить его буквально значило бы
соврать на экране.

1. **Backups → заглушка `COMING SOON`, а не два селекта.** Селекты «Frequency /
   Destination», которые ничего не сохраняют, — это обещание настройки, которой нет.
2. **Logs → реальные четыре файла со своими путями и размерами** (`fp_facts.logs`),
   вместо счётчиков строк (их мы не знаем) и вместо выдуманных строк лога. Тело —
   честное «reading log contents is not wired up yet».
3. **Свежесть снимка на Overview.** Макет ставит «Checked 4h ago · Check on server»
   только на вкладку Server, а карточку SSL кладёт на Overview — при том что SSL там
   читается из ТОГО ЖЕ снимка. Кнопка остаётся одна (на Server), но шапка карточки SSL
   печатает свежесть текстом (`formatAgoStale`), иначе Overview молча выдавал бы
   протухшее измерение за свежее.
4. **Шрифт остаётся текущим** (Inter + системный моно). IBM Plex из макета — это
   вопрос уровня `design-brief.md` §5 ко всему приложению; подключать вебфонт ради
   одной модалки (да ещё сетевой зависимостью в Tauri) неправильно.
5. **Строки «нашей записи»** (`NS:`, `SSL at provision:`, `Last error:`) макетом не
   предусмотрены — остаются на Overview под рядом связей. Причина их появления описана
   в `SSL_PROVISION_ALARM` (`DomainDetailModal.tsx:17-57`) и не отменена.
6. **В мета-строке шапки — только БЕЙДЖ состояния SSL, без «· until 24.04.2041».**
   Макет печатает там срок сертификата, но срок и издатель живут в «Server state», и
   вторая их копия в двух сантиметрах выше — ровно тот дубль, ради снятия которого
   карточка пересобрана планом 17.08. Решение унаследовано от `DomainSummaryBar`
   вместе с его обоснованием (фаза 2).
7. **Пилюли состояний — существующий `Badge`, а не 99px-пилюли макета.** Статус домена
   называет `lib/domainStatus` через `DomainStatusBadge`, состояние сертификата —
   `SSL_BADGE` из `lib/domainFacts`. Второй словарь состояний рядом с первым — ровно
   тот дефект, ради которого эти модули и заведены (фаза 2).
8. **Срок форматирует `lib/domainExpiry`, а не `fmtDate`.** Буква плана в фазе 2
   называла `fmtDate`, но это описка: `expiry_date` приходит датой без времени, и
   `fmtDate` печатает её в зоне читателя, то есть западнее UTC называет предыдущий
   день. Цель самой фразы («чтобы срок в карточке и в колонке списка не разошлись»)
   выполняется как раз `lib/domainExpiry` — им считает колонка списка
   (`DomainRow.tsx:175-178`).
9. **В бейдже чипа Logs у несуществующего файла — слово `missing`, а не прочерк.** Буква
   плана говорила `exists ? size : —`, но тогда один и тот же прочерк означал две разные
   новости: «файла на сервере нет» и «размер прочитать не удалось». Слово уводит различие
   в видимый канал, и прочерк остаётся ровно за вторым случаем.
10. **`formatBytes` заводился в `ui/Primitives.tsx`** — рядом с `formatAgo`/`mbToGb`,
    хотя это чистая функция без React: так сложилось исторически у всех четырёх
    форматтеров, и именно из-за них `snapshotOf` не мог поселиться в `lib/`. **Долг снят
    в фазе 7:** все шесть имён уехали в `lib/format.ts`, шесть импортёров переведены,
    ре-экспорт из `Primitives` не оставлен (шим с нулём потребителей никто никогда не
    удалит), а `snapshotOf` переехал в `lib/domainFacts.ts` — оговорка «поэтому не в
    `lib`» не переписана, а устранена вместе с причиной.
11. **Ряд «свежесть + ошибка проверки + правый слот» — общий `facts/SnapshotLine.tsx`**, а
    не часть `TabLayout`: `TabLayout` знает только геометрию вкладки, а это показ самого
    снимка. Без него вкладка Logs молчала бы о провале своей единственной кнопки: SSH
    падает, `fp_check_error` пишется в БД — и экран остаётся байт в байт прежним.

## Фазы

### Фаза 1 — каркас `[x]`

- **`frontend/src/components/ui/Tabs.tsx`** (новый примитив). Подчёркнутые вкладки по
  спеке макета: 14px, `padding:10px 14px 12px`, активная — 600 `#0f172a` + 2px нижняя
  граница, неактивная — 500 `#64748b`. В отличие от трёх существующих копий
  (`pages/Activity.tsx:71-77`, `pages/Settings.tsx:112-116`,
  `pages/ServerDetail.tsx:888-893` — все `<div onClick>` без ролей) новый примитив
  делает `role="tablist"/"tab"/"tabpanel"`, `aria-selected`, стрелки влево/вправо.
  Существующие три страницы **не трогаем** — это чужой скоуп.
- **`SectionCard`** в `Primitives.tsx` — единый паттерн карточки макета: рамка
  `#dbe3ee`, радиус 12, фон `#fbfcfe`, шапка-полоска `#eef2f7` с uppercase-титулом
  13/700 и правым слотом (пилюля или мета-текст), тело `16px 18px`. Существующие
  `Card/CHd/CTi/CBo` дают другую геометрию и используются десятками мест — новый
  компонент рядом, старые не переписываем.
- **`Modal`** получает необязательный проп `header?: React.ReactNode`, заменяющий
  штатную строку «title + ✕». Дефолт не меняется, остальные вызовы не затрагиваются.
- Ширина карточки 760 → 920. Дисциплина `minmax(0,1fr)` / `minWidth:0` /
  `overflowWrap:"anywhere"` сохраняется — `Modal` держит `overflowY:auto`, из-за чего
  `overflow-x` вычисляется в `auto` (см. долг плана 17.08).

### Фаза 2 — шапка карточки `[x]`

Новый `frontend/src/components/domains/DomainModalHeader.tsx`: eyebrow `DOMAIN` +
`DomainStatusBadge`, имя домена 24/700, мета-строка (SSL-бейдж из `sslState` + срок), ✕
34px. Заменяет `DomainSummaryBar` (его содержимое переезжает в шапку целиком; файл
удаляем, тесты переводим на новую разметку).

Новый `DomainExpiryField.tsx` — инлайн-правка: кнопка с пунктирным подчёркиванием →
`<input type="date">` → `useUpdateDomain({expiry_date})`, закрытие по blur, пустое
состояние «set date». Формат вывода — существующий `lib/domainExpiry` + `fmtDate`, чтобы
срок в карточке и в колонке списка не разошлись.

**Долг №4 плана 17.08 закрывается здесь заодно:** `sslState` считается один раз в
модалке и раздаётся вниз — теперь его потребителей два (шапка и карточка SSL на
Overview), и «сойдётся само» перестаёт быть приемлемым.

### Фаза 3 — вкладка Overview `[x]`

`components/domains/tabs/DomainOverviewTab.tsx`:

- Ряд связей — существующий `DomainLinks` (+ `DomainRegistrarField`,
  `DomainCloudflareField`) в трёх `SectionCard`. Логику полей не трогаем.
- Строки «нашей записи» под рядом (см. отступление 5).
- **SSL** — `SectionCard` с kv-сеткой `80px 1fr`: State (бейдж), Expires, Issuer. Это
  SSL-часть, вынутая из `DomainServerFacts`, вместе с её `FactRow`/`DRAW`-логикой
  расхождений. В правом слоте шапки — свежесть снимка (отступление 3).
- **Nameservers** — существующий `DomainNsPanel` в `SectionCard`, пилюля делегирования
  (`nsDelegation`) переезжает в правый слот шапки. Textarea, «Restore from Cloudflare»,
  push-кнопка, строки-объяснения и ошибка из `MutationCache` сохраняются полностью.
- **DMCA** — заглушка с пилюлей `COMING SOON`, текст из макета.

### Фаза 4 — вкладка Server `[x]`

`DomainServerFacts.tsx` распиливается. Общее (`FactRow`, `DRAW`, `HasSnapshot`, `Row`,
`Note`, `SubTitle`) **уже уехало в `domains/facts/` в фазе 3** — второй потребитель
появился там, у карточки SSL, и дублировать ради одной фазы правило расхождений было бы
ровно тем дефектом, который этот репозиторий чинит третий раз. Файл после фазы 3 —
400 строк, а не 568.

- Верхняя строка: `formatAgoStale` слева, `Проверить на сервере` (desktop-only) справа.
- `fp_check_error`-алерт и легенда `noSnapshot` — как есть.
- **FTP Access** — Host (IP сервера), Port 21, Login, Password (`FtpPassword` +
  `RevealSecret` + «Задать пароль»), плюс перечень «Accounts on server» под гейтом
  `ftpRosterAdds`.
- **Site** — Path, Owner, PHP, Databases, DB user. Строка `Logs` отсюда уходит на
  вкладку Logs (дубля быть не должно).
- **Backups** — заглушка `COMING SOON` (отступление 1).

### Фаза 5 — вкладка Logs `[x]`

`tabs/DomainLogsTab.tsx`. Чипы по `fp_facts.logs[]`: имя файла выводим из пути
(`-frontend.access.log` → «Frontend access»), в бейдже — размер (`exists ? size : —`),
не число строк. Под чипами — полный путь моно 12px. Тело — честное состояние: «Reading
log contents is not wired up yet», а при `!fp_facts_at` — «Never checked» с той же
кнопкой снятия снимка, что на Server. Тёмная консоль и таблица access из макета
реализуются, когда появится чтение (отдельная функция: новая Tauri-команда с `tail` по
SSH; на бэкенд это не ложится — zero-knowledge, SSH-пароль это блоб).

### Фаза 6 — Template и Custom `[x]`

По одной заглушке-`SectionCard` на вкладку, тексты по образцу DMCA. Custom объясняет,
чем станет: «Presets - saved multi-step actions you can run on this domain».

### Фаза 7 — тесты `[x]`

Существующие наборы (`DomainDetailModal.overview.test.tsx` 356,
`.cfzone.test.tsx` 701, `.setns.test.tsx` 679, `DomainServerFacts.test.tsx` 540)
сломаются: содержимое ушло под вкладки. Правка — хелпер `openTab(name)` в общем
`test-utils` и точечные вставки, **без переписывания утверждений**: они охраняют
поведение, которое эта работа менять не должна.

Новое покрытие: клавиатура и роли `Tabs`; правка срока (оптимистичный рендер, ошибка
мутации, пустое состояние); чипы Logs на снимке и без снимка; неразъезжаемость
SSL-бейджа шапки и карточки SSL (один `sslState` на модалку).

## Проверка

```bash
cd frontend && npm test -- --run
```

```bash
cd frontend && npx tsc --noEmit
```

Живая приёмка (jsdom геометрию не считает вовсе — это записано в плане 17.08):

1. Ряд связей на 920px с длинными именами аккаунтов CF и регистратора и IPv6 —
   горизонтальной полосы у модалки быть не должно.
2. Здоровый домен: «Проверить на сервере» → строк «при развёртывании: …» ни одной,
   бейдж SSL в шапке и State в карточке SSL совпадают.
3. Домен без снимка: Overview показывает свежесть «Never checked», Logs — то же
   состояние, а не пустые чипы.
4. Правка срока: клик → дата → blur; значение совпало с колонкой Expires в списке
   доменов, после `F5` осталось.
5. Переключение вкладок с клавиатуры (Tab на таблист, стрелки между вкладками).

## Итог

Реализован **целиком**: фазы 1–7 выполнены. Осталась живая приёмка человеком.

**Фаза 1 — каркас.** Примитив `ui/Tabs.tsx` с ролями `tablist`/`tab`/`tabpanel`,
`aria-selected`, roving tabindex, ходьбой стрелками и `Home`/`End`; `SectionCard` рядом
с `Card`/`CHd`/`CTi`/`CBo`; необязательный проп `header` у `Modal`; ширина карточки
760 → 920. Ревью вскрыло два настоящих дефекта, оба закрыты тестами, которые падают при
откате правки: неизвестный `value` выбрасывал ВСЮ строку вкладок из таб-порядка (теперь
`activeIdx` отвечает за выбор, `focusIdx` — за точку входа с клавиатуры), а `header ??`
вместо `header ||` оставлял модалку без крестика при `header={cond && …}`. Записана и
цена ленивой отрисовки: переключение вкладки размонтирует поддерево, поэтому состояние,
обязанное пережить переключение, поднимается в модалку.

**Фаза 2 — шапка карточки.** `DomainModalHeader` (eyebrow `DOMAIN` + статус, имя домена
`<h2>` 24/700, мета-строка, свой крестик 34px) заменил `DomainSummaryBar`, файл удалён.
`DomainExpiryField` правит срок инлайном. Долг №4 плана 17.08 закрыт наполовину
осознанно: `sslState` считается один раз в модалке и уезжает в шапку пропом, а
`DomainServerFacts` до фазы 3 считает свой — переносить его раньше значило бы затащить
работу фазы 3 в этот коммит.

Правка срока оказалась сложнее, чем звучала в плане, и её итоговое устройство —
результат двух кругов ревью:

- **Запись по `change` стирала срок посреди набора с клавиатуры.** `<input type="date">`
  отдаёт `""` у любой недобранной даты, поэтому backspace через сегмент отправлял
  настоящий `{expiry_date: null}`; два запроса из одного хука ещё и гонялись, и
  приземлившийся вторым `null` оставлял базу пустой при уверенно показанной новой дате.
  Открытое поле стало **черновиком** (`draft`): `change` пишет только полную дату,
  `blur` подтверждает только снятие. Безусловный commit на `blur` пробовали — он
  молча ПОВТОРЯЛ отклонённую запись.
- **Наложение «показываем выбранное, пока не приземлилось»** сравнивает не «строка ≠
  выбранного», а «строка ещё та же, поверх которой правили» (`edit.base`). Иначе чужая
  правка `expiry_date` (синхронизация регистратора, write-back full-setup) воскрешала
  бы наложение законченной правки на всю жизнь модалки.
- **Тесты «ничего не записано» были пусты дважды**: `mutate` зовёт `mutationFn`
  асинхронно, и синхронное утверждение проходило даже при состоявшейся записи. Лечится
  хелпером `settle()` перед каждым таким утверждением — без него удаление проверяемого
  гарда не роняло ни одного из 16 тестов.

Проверка на момент закрытия фазы 2: `npx tsc --noEmit` чист, `npx vitest run
--no-file-parallelism` — 102 файла / 1153 теста, все зелёные. **Прогон обязан быть
серийным**: параллельный на этой машине сыпет ложными таймаутами по 5с с обеих сторон
любого диффа.

Отступление от буквы плана: тесты клавиатуры и ролей `Tabs` написаны сразу в фазе 1, а
правка срока — в фазе 2, хотя план относил и то и другое к фазе 7. Примитив и поле
уезжают в продукт со своим контрактом, и запирать его через пять фаз значило бы пять фаз
не иметь на него доказательства.

**Фаза 3 — вкладка Overview.** Карточка стала вкладочной: `Tabs` в модалке с двумя
вкладками — Overview и Server. Пустых Logs/Template/Custom не заводили намеренно:
вкладка, открывающаяся в никуда, — то самое обещание несуществующего, от которого этот
план отказывается. Overview собран из трёх `SectionCard` ряда связей, строк «нашей
записи», пары SSL | Nameservers и заглушки DMCA; `DomainSslCard` вынут из
`DomainServerFacts` вместе с логикой расхождений, свежесть снимка печатается в правом
слоте его шапки (отступление 3), кнопка проверки осталась одна — на Server. Пилюля
делегирования переехала в правый слот шапки карточки NS.

Что вскрыли два круга ревью:

- **`HasSnapshot` отвечал на два разных вопроса.** Он гасил и пустую строку («снимка
  нет»), и приписку `recorded-only` — но второе не свойство снимка, а свойство «легенда
  тремя строками выше уже сказала это дословно», верное только у секции сервера.
  Разъехалось бы на первом же новом потребителе. Разведено на `HasSnapshot` (прячет
  строки) и `RecordedNoteInLegend` (гасит приписку, ставится тем, кто рисует легенду).
- **Приписка «из provision, на сервере не проверено» под КАЖДОЙ строкой — не лечение.**
  Первая правка напечатала её дважды в карточке шириной 389px, в десяти пикселях друг
  от друга; на самом частом экране продукта (только что развёрнутый домен) оба поля как
  раз `recorded-only`. Стало — одна легенда на карточку, по-английски (правило языка
  плана; русская легенда секции сервера — прежний долг, а не образец).
- **Правило свежести копировалось в третий раз.** Пара «`fp_facts_at` открывает
  `fp_facts`» + `isFactsStale` + строка `Checked …` / `Never checked` уехала в
  общий модуль. Тогда — `domains/facts/snapshot.ts`, потому что `freshness` звал
  `formatAgoStale` из `ui/Primitives`, а `lib` компонентов не знает нигде; в фазе 7
  причина снята, и правило переехало в `lib/domainFacts.ts`.
- **Черновик NS переживает переключение вкладки.** `Tabs` рисует только активную
  панель, поэтому Overview → Server → Overview размонтировал бы `DomainNsPanel` и
  стирал набранный, но не отправленный список. Состояние поднято в модалку хуком
  `useNsDraft`; черновик несёт с собой и тот список зоны, против которого он сверяется,
  — иначе «Restore from Cloudflare» сравнивалось бы с одним списком, а зеркалилось с
  другим.
- **Мутационное тестирование поймало то, чего не поймал ни один экранный тест:** возраст
  снимка, посчитанный от `fp_checked_at` вместо `fp_facts_at`, проходил все 116 тестов —
  экран сказал бы «Checked 1m ago · stale» под данными недельной давности. Утверждение
  теперь на всю строку, а не на суффикс. Отдельный файл `facts/fields.test.tsx` заведён
  потому, что после правки легенды два контекста стали неразличимы на всех состояниях,
  достижимых с экранов, — различает их только прямой тест `FactRow`.

Проверка на момент закрытия фазы 3: `npx tsc --noEmit` чист, серийный прогон —
105 файлов / 1175 тестов, все зелёные.

**Фаза 4 — вкладка Server.** `DomainServerFacts.tsx` стал тремя файлами:
`tabs/DomainServerTab.tsx` (раскладка, верхняя строка со свежестью и кнопкой,
`fp_check_error`-алерт, легенда `noSnapshot`, оба контекста и заглушка Backups),
`DomainFtpCard.tsx` и `DomainSiteCard.tsx`. Обе карточки получают уже разобранный
`Snapshot` пропом, а не зовут `snapshotOf` заново, и тип прописан по тому, что карточка
ЧИТАЕТ (`Pick<Snapshot, "facts">` у FTP, `+ "noSnapshot"` у Site) — карточка, начавшая
печатать вторую свежесть, теперь не компилируется. Строка `Logs` убрана: её предмет —
фаза 5. `SubTitle` умер, как и обещал его собственный комментарий.

Что вскрыло ревью:

- **Пустая карточка Site у импортированного домена** (ни снимка, ни записи provision)
  выглядела сбоем: рамка, крашеный титул и — из-за `align-items: stretch` в новом ряду —
  высота соседней заполненной карточки при пустом теле. Стало — одна честная фраза
  «No site details recorded for this domain yet.» под точным условием `noSnapshot &&
  все `src` в `agree``; ревьюер проверил его на восьми состояниях, включая «есть только
  запись FTP» (фраза остаётся: запись FTP — не запись о сайте) и «запись из одних
  пробелов» (тоже не запись).
- **Флаговый аргумент `listFact(value, noSnapshot)`** держал инвариант, который ничто не
  проверяло: `listFact(x, hasSnapshot)` компилировался и молча инвертировал смысл.
  Функция удалена, поведение уехало в сам `FactRow` (`list`), и перепутать нечего.
- **Пароль FTP стал молча обрезаться.** `RevealSecret` печатает плейнтекст в `<code>` без
  правила переноса; в карточке шириной 389px под `overflow: hidden` длинный пароль
  срезался — раньше та же строка давала модалке горизонтальную полосу. Починено на месте
  вызова, общий компонент не тронут.
- **Оболочка вкладки копировалась дословно** (отступ, `gap: 14`, восьмистрочный абзац про
  `minmax(0,1fr)`). Вынесена в `tabs/TabLayout.tsx` (`TabBody`, `CardRow`, `TabGroup`) до
  того, как копий стало пять.
- **`minWidth: 0` у трёх внутренних сеток не работал вовсе**, а объяснение при нём
  называло механизм, который не действует: `min-width: auto` содержимым меряется только у
  flex- и grid-элементов, а это блочные дети блочного тела `SectionCard`. Снято из трёх
  файлов; у `<span>` вокруг `RevealSecret` оставлено — вот он как раз flex-элемент.
- **Десять комментариев в семи файлах** продолжали называть «Server state» живым
  элементом экрана, снятым этой же фазой.

Отдельно: реализатор дважды принял выживший мутант за эквивалентный, оба раза потому что
гонял мутацию по своей папке, а не по всей сюите (в первый раз правило убивал тест
соседней карточки, во второй — мутация была наложена не на то выражение). Правило —
мутации проверяются полным серийным прогоном.

Проверка на момент закрытия фазы 4: `tsc` чист, 105 файлов / 1182 теста, все зелёные.

**Фаза 5 — вкладка Logs.** Третья вкладка: чипы по `fp_facts.logs[]` (подпись выводится
из пути, а не из захардкоженной четвёрки), полный путь под ними, честное тело. Тёмная
консоль и таблица access из макета не реализованы — читать содержимое пока нечем, и это
отдельная функция (новая Tauri-команда с `tail` по SSH; на бэкенд не ложится —
zero-knowledge, SSH-пароль это блоб). Кнопок «Refresh»/«Download» из макета нет: кнопка,
которая ничего не делает, — то самое обещание, от которого план отказывается.

Что вскрыло ревью:

- **Экран ставил диагноз, которого фронт знать не может.** Пустой `logs[]` объяснялся
  фразой «последняя проверка не нашла домашний каталог» — но пустой список приезжает
  ЧЕТЫРЬМЯ путями: владелец не найден (`fastpanel_facts.rs:615`), найден, но пустой
  (`:537`), команда упала — код возврата там осознанно выброшен, а непарсящийся вывод
  даёт пустой список внутри УСПЕШНОГО снимка (`:542-544`, `:502-528`), и снимок старой
  формы, где поля `logs` нет вовсе (`?? []`). Фраза оставила утверждение о нашем
  незнании и выбросила утверждение о причине.
- **Единственная кнопка вкладки молчала о своём провале.** `runReadDomainFacts` наружу
  ошибку не отдаёт намеренно — пишет `fp_check_error` в БД, чтобы её показал экран;
  Server показывал, Logs нет. После падения SSH экран Logs был байт в байт прежним.
- **Ряд «свежесть + кнопка» скопирован дословно** вместе с третьим литералом `#8a8580`
  при экспортированном `STALE_TEXT`. Сведён в `facts/SnapshotLine.tsx` — заодно закрыв
  предыдущий пункт.
- **`formatBytes` печатал `1024 KB`** (и `1024 MB`, и `1024 GB`): округление стояло после
  цикла, окно 512 байт под каждой ступенью. Лог в мегабайт-минус-байт — не экзотика.
- **Комментарий обещал три канала, а код закрывал один:** различие «файла нет» жило в
  `title`, про который было написано, что он спасает печать и чёрно-белый скриншот. `title`
  не доезжает туда ровно так же, как цвет. Различие уехало в видимый бейдж (отступление 9).

Проверка на момент закрытия фазы 5: `tsc` чист, 108 файлов / 1221 тест, все зелёные.
Мутационная матрица воспроизведена ревьюером целиком плюс три собственных мутанта на
новый шов; единственный выживший (`null` в `formatBytes`) честно эквивалентен по рантайму
и запрещён типом — проверено падением `tsc`.

**Фаза 6 — Template и Custom.** Две заглушки той же формы, что DMCA и Backups: `SectionCard`
+ пилюля `COMING SOON` + одна фраза. Из макета не реализовано ничего — ни селекта шаблонов
с одноразовым выбором, ни фальшивого превью в окне браузера, ни сетки из девяти пресетов с
кнопками «Run», ни вложенной модалки «+ New preset»: за всем этим нет ни модели, ни поля,
ни команды.

Главная ловушка фазы — **`nginx_presets`**. Колонка в БД есть (`models/domain.py:61`), в
схемах есть (`schemas/domain.py:75,116`), в Rust есть рендер (`fastpanel.rs:876`,
`render_nginx_snippet`, ровно четыре флага) — и ни одного вызова: `apply_nginx_override`
не зовёт никто, то есть колонку сегодня не читает вообще ничто. Но это **не то же самое**,
что «пресеты» макета: там сохранённые многошаговые ДЕЙСТВИЯ («Change NS records»,
«Reissue SSL»), здесь четыре булевых флажка конфига nginx. Совпадает одно слово. Заглушка
колонку не читает и на неё не намекает, а комментарий гасит ошибку, которую следующий
читатель сделал бы сам («в базе же лежат пресеты, давайте выведем»).

Ревью нашло одну вещь: тот же коммит, который переписал классовый комментарий модалки,
**опроверг соседний комментарий в `ui/Tabs.tsx`** — там было сказано, что панель не делают
фокусируемой, потому что внутри поля и кнопки; у Template и Custom внутри не фокусируется
ничто, и это не случайность, а смысл заглушки. Посылка сужена, оговорка названа, условие
возврата записано: если заглушка вырастет до текста, который надо ЛИСТАТЬ, панели нужна
настоящая история фокуса, и чинить это в примитиве.

Заодно утверждение «вкладки про NS нет» в `setns.test.tsx` переведено с равенства всего
списка на поиск по образцу `/\bNS\b|nameserver|dns|delegation/i`: равенство краснело от
любой посторонней вкладки, и правка сводилась к вставке строки не глядя.

Проверка на момент закрытия фазы 6: `tsc` чист, 108 файлов / 1224 теста, все зелёные.

**Фаза 7 — аудит покрытия и уборка.** Список «нового покрытия» из плана к этому моменту
почти весь уже приземлился в фазах 1–6 (клавиатура и роли `Tabs` — в фазе 1, правка срока
— в фазе 2, чипы Logs — в фазе 5), поэтому фаза свелась к трём работам.

**Аудит.** 24 мутации, каждая — на полном СЕРИЙНОМ прогоне. 21 умерла на существующих
тестах, **три выжили, и все три были настоящими дырами**: гейт `fp_facts_at` у чипов
Logs, и — дважды — потребитель, который вместо пропа считает `sslState` сам. Последнее
невидимо изнутри модалки по построению: аргументы те же, ответ тот же, все тесты зелёные.
Лечится тестом, где снимок ПРОТИВОРЕЧИТ пропу. Ревью нашло четвёртую того же класса:
реализатор проверил оба конца провода и не проверил сам провод — подмена `ssl={ssl}` на
пересчёт в `DomainOverviewTab` переживала все 1227 тестов.

Заодно вскрылось, что **сама `SSL_BADGE` не была закреплена ничем**: три новые таблицы
брали ожидаемый ярлык из неё же, так что смена `unchecked.variant` на зелёный не роняла
ни одного теста — то есть правило принципа №6, ради которого модуль и заведён, держалось
на честном слове. Карта закреплена целиком (`toEqual`), а таблицы стали
`it.each(Object.keys(SSL_BADGE))`: потребитель, подменивший ОДНУ ступень лестницы, теперь
умирает на своей строке.

**Уборка.** `formatBytes`, `formatAgo`/`formatAgoStale`, `mbToGb`, `formatUptime` и
`STALE_SUFFIX` уехали из 599-строчного `ui/Primitives.tsx` в `lib/format.ts` (модуль
похудел до 443), шесть импортёров переведены, ре-экспорт снят за ненадобностью, тесты
переименованы вслед за предметом. `snapshotOf` переехал в `lib/domainFacts.ts` — и
подпись у него стала `(facts, factsAt, now)` вместо `(domain, now)`: с `Domain` он
заводил ребро `lib → api` и спорил с политикой соседа (`lib/domainDrift` прямо
объявляет, что типа `Domain` не знает намеренно).

**Финальное ревью всей ветки.** Проверило перенос строкой: из всех строк, которые
карточка печатала до ветки, потерялась ровно одна — подзаголовок `FTP access` стал
титулом карточки `FTP Access`. Пятнадцать собственных состязательных мутаций по швам
между фазами: тринадцать убиты, несколько — тестами из ДРУГОГО файла. Две выжившие
(подпись ротированного лога и связь панели с вкладкой после переключения) закрыты двумя
строками тестов. Плюс два комментария, которые эта же ветка сделала ложными: обоснование
API примитива `Tabs` ссылалось на запросы вкладок, которых у них нет (все запросы
карточки живут в модалке и стреляют независимо от вкладки), а шапка отправляла читателя
за сроком сертификата в секцию сервера, откуда фаза 3 его унесла.

Проверка на момент закрытия: `npx tsc --noEmit` чист, серийный прогон — **110 файлов /
1252 теста**, все зелёные (было 1122 до начала работы).

## Долг, собранный по дороге (не этой работы)

Ни один пункт не относится к переносу дизайна; всё это найдено попутно и записано, чтобы
не потерялось.

1. **`ns_status ?? "pending"`** (`tabs/DomainOverviewTab.tsx`) печатает слово *pending*
   домену, о NS которого у нас нет записи вовсе, — незнание, нарисованное состоянием
   (принцип №6). Строки переехали дословно, поэтому здесь не тронуты.
2. **`nginx_presets` — мёртвая поверхность продукта.** Колонка (`models/domain.py:61`),
   два поля схем (`schemas/domain.py:75,116`), миграция `010_domain_extras.py`, поле типа
   во фронте (`api/domains.ts:39`) и рендер в Rust (`fastpanel.rs:876`) — при том что
   `apply_nginx_override` (`:912`) не зовёт никто. Надо решить: дописать или удалить.
   Внимание: «пресеты» вкладки Custom — это НЕ она (там многошаговые действия, здесь
   четыре флажка nginx), совпадает одно слово.
3. **`RevealSecret.tsx:60,66`** — `<code>` с плейнтекстом и чужой текст ошибки без правила
   переноса. Фаза 4 обернула место вызова в карточке домена; остальные потребители
   компонента по-прежнему обрежут длинный пароль или ошибку с URL.
4. **Двуязычие интерфейса:** `Проверить на сервере` (теперь на двух вкладках),
   `Задать пароль`, легенда `noSnapshot` — русские строки на англоязычных вкладках. Это
   долг №9 плана 17.08, намеренно не лечился (решение 3 этого плана).
5. **Стоячее условие в `ui/Tabs.tsx`:** если панель-заглушка когда-нибудь вырастет за
   одну целиком видимую строку, ей понадобится настоящая история фокуса, и чинить это в
   примитиве явным пропом, а не в заглушке.
6. **`fmtDate`/`fmtDT` остались в `ui/Primitives.tsx`** — после уборки фазы 7 это
   единственные чистые форматтеры в компонентном модуле. Дом у них не автоматический:
   даты в `lib` уже живут в `lib/domainExpiry` (`formatExpiryDate` с правилом про UTC),
   так что решать между ним и `lib/format` надо до появления третьей реализации.
7. **Три самодельные строки вкладок** — `pages/Activity`, `pages/Settings`,
   `pages/ServerDetail`: `<div onClick>` без ролей и без `tabindex`, недостижимые с
   клавиатуры. Примитив `ui/Tabs` теперь существует как их адрес; переезд не начат и
   ничей.
8. **Цвета бейджа SSL в `DomainRow.tsx`** остались тернарником при живой лестнице в
   `lib/domainStatus`. Комментарий самого файла называет этот долг «живым, но ничьим»:
   переезд означает переписать разметку ячейки, то есть отдельную задачу.
9. **Третий адрес отступа вкладки:** `DomainLinks.tsx` рисует свой
   `repeat(3, minmax(0,1fr))` с литералом `gap: 14`, тогда как отступом тела вкладки
   владеет `TabLayout.TAB_GAP`. Числа колонок у `CardRow` нет намеренно — до первого
   потребителя; ряд связей и есть тот потребитель.

## Без тестового покрытия — только живая приёмка

jsdom геометрию не считает вовсе, поэтому зелёный прогон НИЧЕГО не говорит про: ширину
920 и отсутствие горизонтальной полосы, перекрытие подчёркивания строки вкладок,
переключение вкладок с клавиатуры на живом экране, отступ панели вкладки, `overflowWrap`
у чужих текстов в панели NS и `useId` у её поля (второй панели NS на экране не бывает).

**Живая приёмка человеком (5 пунктов раздела «Проверка») не проводилась ни разу.**
