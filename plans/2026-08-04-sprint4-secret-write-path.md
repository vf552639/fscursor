# Спринт 4 — путь записи блоба секрета (разблокировка фазы A)

Источник: `sprint4.md` в корне. Долг Спринта 3 (12 пунктов) —
`plans/2026-08-03-sprint3-ui-reachability-writeback.md`, итоговый блок.

## Проблема

Пути записи блоба секрета не существует нигде: `encryptBlob` без вызовов,
`vault_put_blob`/`vault_delete_blob` только зарегистрированы, формы шлют плейнтекст в поля,
которых нет в схемах (`extra="ignore"` по умолчанию) → 200 OK и `*_blob_id = NULL`. Ни одна
из 26 Tauri-команд не может отработать.

## Принятые решения (зафиксированы до старта)

1. **Шифрует Rust, не браузер.** `vault_put_blob(user_id, blob_id, blob_kind, plaintext_b64)`
   уже принимает **плейнтекст** и шифрует его `aead::encrypt` мастер-ключом из keychain
   (`commands/vault.rs:29-48`). В `sprint4.md` описан вариант с `encryptBlob` во фронте —
   он расходится с реальной сигнатурой. Берём существующую: плейнтекст не покидает машину
   (Tauri IPC), ключ живёт только в keychain десктопа, во фронт мастер-ключ тянуть не надо.
   `encryptBlob` в `crypto.ts` остаётся тестовым хелпером паритета с Rust.
2. **Секреты вводятся только в десктопе.** Вне Tauri форма с секретом упирается в
   `requireDesktop(...)` — тот же текст, что у остальных «только десктоп» действий.
3. **Baseline тестов на старте спринта:** pytest 51 passed / 1 failed
   (`test_mutation_audit.py::test_config_key_owned_by_another_user_cannot_be_overwritten` —
   падал **до** спринта), vitest 165 passed, cargo 112 passed.
4. **Реальный SSH-коннект в приёмке — ручной шаг.** Автотесты доказывают эффект по БД
   (ciphertext в `blobs`, `*_blob_id` заполнен, расшифровка возвращает исходный секрет);
   подключение к живому серверу проверяет человек по чек-листу в конце плана.
5. Фаза 6 (`долг №7–12`) вынесена в отдельный cleanup-спринт — по решению `sprint4.md`.

## Фазы

- [ ] **1a. Тонкий срез: SSH-пароль сервера.**
  - [x] T1 — `frontend/src/lib/secretBlob.ts`: `putSecretBlob` / `deleteSecretBlob` + юнит-тесты.
        Коммиты `ac18338`, `40821e2`, `e9309e2`. Оба ревью пройдены (spec + качество).
  - [x] T2 — проводка SSH-пароля: `pages/Servers.tsx` (вкладка install), `pages/ServerDetail.tsx`.
        Коммиты `ed658d6`, `ee14b86`, `c208165`. Spec-ревью пройдено дважды;
        **финальное ре-ревью качества по `c208165` не проводилось** — начать с него.
  - [x] T3 — приёмка по эффекту: backend-тест (ciphertext в БД, `ssh_password_blob_id`
        заполнен) + Rust-тест круга шифрования блоба. Мутационная проверка.
        Коммиты `e0f7cf2`, `8c293d9`. Попутно вскрыта и закрыта дыра: мутация
        `let ct = pt.clone()` в `vault.rs` оставляла все 115 Rust-тестов зелёными —
        шаг шифрования разъят в `put_blob_encrypted` и теперь покрыт.
- [ ] **1b. Развернуть паттерн** — `fastpanel_password`, регистратор (`api_key`/`api_secret`),
      Cloudflare (`api_token`); правка секрета = перезапись того же `blob_id`, удаление
      сущности → `vault_delete_blob`.
  - [x] 1b-1 (фундамент) — коммиты `dc06d06`, `5cc8a5a`. `useMultiSecretSave`/`saveAll` на
        несколько секретов под один POST; `persist` сужен до `Promise<void>`;
        `RegistrarAccountResponse` отдаёт `api_key_blob_id`/`api_secret_blob_id` (без них
        форма правки не знала бы, какой блоб перезаписывать). **Spec-ревью пройдено с
        находкой, находка закрыта; ре-ревью по `5cc8a5a` и ревью качества не проводились —
        начинать с них.**
  - [x] 1b-2a — коммиты `d835c52`, `8a9d2b8`, `2e79ebe`. Пять находок ревью по хуку +
        `fastpanel_password` вкладки connect; `fastpanel_password` удалён из TS-типов.
        Оба ревью пройдены.
  - [x] 1b-2b — коммиты `083f452`, `20d8937`, `c8aa907`, `063073f`, `f01b35e`, `e645f85`,
        `056720c`. Cloudflare `api_token`, регистратор `api_key`/`api_secret` (первый
        реальный вызывающий `saveAll`), поле Client IP у Namecheap (десктоп его читал, а
        UI не было), удаление сущности снимает её блобы, плейнтекст-поля удалены из
        TS-типов, add-модалки вынесены из страниц. Оба ревью пройдены.
        **Хвост на 1-3 строки, закрыть до конца фазы:** комментарии
        `Cloudflare.tsx:737-738` и `Settings.tsx:397` обещают страховку, которой нет
        (обработчик тримит локальную копию, а в блоб уезжает стейт хука); тесты подают
        `"   "`, что отбивается и вводом, и обработчиком, поэтому снятие `.trim()` из
        одного `onChange` не роняет ничего — подать `"  token  "` и утверждать
        `blobPlaintext(...) === "token"`. Плюс мелочи ревью: индекс первого
        `vault_delete_blob` вместо первого любого вызова в `expectBlobsGoneAfterEntity`;
        `expect(result.current.error).toBeNull()` в `expectDeleteIgnoresBlobFailure`;
        вернуть в JSDoc `forgetSecretBlobs` довод, почему провал уборки не показываем.
- [x] **2. Громкий провал** — `extra="forbid"` на `ServerCreate/Update`, `CloudflareAccount*`,
      `RegistrarAccount*`; плейнтекст-поля вон из TS-типов и тел запросов.
      Коммиты `049a456`, `4aba1bc`, `06d7d92`, `1fd8e36`, `a3f39aa`, `0e029aa`, `ad0831a`,
      `dfda66a`, `2a7b03b`, `243b16c`. Spec-ревью и оба круга ревью качества пройдены.
  - [x] `model_config = ConfigDict(extra="forbid")` стоит на каждом из шести Create/Update
        отдельно. Не потому, что на Base нельзя (можно: `extra="forbid"` не мешает
        `model_validate` ORM-объекта — вход не dict), а потому, что одна правка на Base
        гасила бы ловушку сразу у шести и красное говорило бы «сломано где-то».
  - [x] `tests/test_secret_write_path.py` — оба старых теста шлют `ssh_password` и теперь
        ждут `422` + `loc == ["body","ssh_password"]`; поле из тела НЕ убрано, чистый POST
        добавлен рядом, чтобы перебор колонок остался на живой строке.
        `test_plaintext_secret_field_is_rejected_loudly` — шесть случаев, по одному на схему.
        Мутационная проверка: снятие `forbid` с каждой схемы роняет ровно её случай (6/6).
  - [x] Плейнтекст-полей в TS-типах и телах запросов не осталось ещё с фазы 1 — аудит всех
        вызывающих (фронт ×7 тел, `ServerWriteBack` десктопа, `bulk_import_service`,
        backend-тесты) расхождений со схемами не нашёл.
  - [x] Побочная находка, закрытая здесь же: дефолтный 422 FastAPI возвращает `input` —
        то есть присланный плейнтекст. Оттуда он уезжает в `ApiError.details`
        (`api/client.ts` кладёт туда тело ответа целиком), в состояние ошибки React Query
        и в логи; в десктопе — в `ApiError::Status { status, body }` (`sync/http.rs`).
        `_input_is_unsafe_to_echo` + `validation_error_without_secret_input` в
        `app/main.py` снимают `input` по четырём признакам: `extra_forbidden`;
        `input`-контейнер (у `missing` pydantic кладёт туда всё тело); ошибка про корень
        тела (`loc == ["body"]` — двойная сериализация даёт тело-СТРОКУ, контейнерный
        признак её не ловит); секретоподобное ИМЯ поля (`SECRET_NAME_MARKERS` — та же
        конвенция, что у десктопного `audit_redact.rs`, плюс `_b64`, `_blob_id`, `token`
        и `secret`: `auth_key_b64` короче минимума отдаёт `string_too_short` с ключом
        внутри, а `ssh_password_blob_id` с плейнтекстом — `uuid_parsing` с паролем).
        Сплошной обход поверхности запросов ревьюером: 126 полей в телах, 27 глушатся,
        ложноположительное одно (`salt_b64`, соль секретом не является) — записано в
        комментарий рядом со списком.
        Признаки 2-4 — находки ревью. Скалярный `input` несекретных полей сохранён,
        на это есть свой тест. Мутационно проверен каждый признак по отдельности.
  - [x] Уборка за тестами: `test_secret_write_path.py` не удалял пользователей вовсе
        (в общей dev-БД накопилось 285 строк). Автоюзная фикстура + `_purge_users`,
        всё хозяйство уезжает по FK CASCADE; проверено тестом и счётчиком в БД.
- [ ] **3. Опасные пути** — `sdmp://bulk-provision` через гейт подтверждения с возвратом
      результата (№3); проверки существования БД/FTP-аккаунта до создания (№5).
  - [x] **3a — `sdmp://bulk-provision` (долг №3). ЗАКРЫТА.** Spec-ревью и оба круга ревью
        качества пройдены. Коммиты: `9ec7275` (Rust — отчёт вместо ключа), `0d13b23`
        (фронт — гейт и проводка), `2f95c54` (план), `5d8cb01` (две строки, ставшие
        ложными), `4cfc33e` (осиротевший `running`, `create_ftp_account`, именованные
        константы), `d0ec1b4` (холостой тест на кэш мутаций), `5d3a991` (итог экраном,
        счётчик очереди, backdrop, мелочи), `2ca4a59` (план), `266ffe8` (сужение типа,
        тест backdrop у FastPanel, порядок с `fpQueue`).
        Тесты на выходе: cargo **126 passed**, vitest **257 passed / 32 files**.
  - [x] 3a — `sdmp://bulk-provision` (долг №3). Коммиты `9ec7275` (Rust), `0d13b23` (фронт).
        Часть «мимо гейта подтверждения» оказалась уже неверной: `handleSdmpDeepLinkInTauri`
        подтверждает все три действия с прошлого спринта. Остальное было в силе.
        - `provision_bulk` возвращает `BulkProvisionOut` вместо ключа идемпотентности:
          строка на каждый домен с тремя различимыми исходами (`done` с
          `ProvisionResultOut`, `failed` с текстом, `skipped` — не запускался), общий
          статус (`ok`/`failed`/`already_ran`) и причина обрыва.
        - **Останавливаемся на первой ошибке** (решение зафиксировано в коде): провал
          массового прогона почти всегда системный — запертый keychain, недоступный сервер,
          протухшие креды, — и «идти дальше» означает N × минуты SSH-таймаутов до первого
          слова пользователю. Цена снята тем, что хвост помечен `skipped` поимённо, а всё
          успевшее уезжает наружу вместе с ошибкой: повтор идёт ровно по хвосту.
        - Частичный провал и сбой записи финального статуса больше не съедают пароли
          отработавших доменов (было два `return Err`).
        - Ранний возврат по идемпотентности отдаёт `already_ran`, а не пустой успех.
        - Гейт: `runBulkProvisionDomains` закрывает обе стороны подоменного гейта —
          набор не стартует, если по какому-то его домену provision уже летит, и на время
          прогона на каждый домен набора заводится собственная pending-заявка в
          `MutationCache` под `PROVISION_DOMAIN_KEY` (её видит ⚙ страницы `Domains` и
          `runProvisionDomain`). Заявка возвращает только id: пароли в `data` мутации
          пережили бы `reset()` ещё gcTime.
        - Проводка наверх: `DeepLinkOutcome.bulkProvision` → воркспейс кладёт каждый
          результат в ту же `useShowOnceQueue`, что и одиночный provision, и тостит
          `summarizeBulkProvision` (чистая функция, намеренно не касается `result`).
        - Тесты: cargo **120 passed** (было 115, +5), vitest **248 passed / 32 files**
          (было 239/31, +9). Мутационно проверен каждый новый тест — журнал ниже.
  - [x] 3a-fix — правки по ревью качества. Коммиты `4cfc33e` (Rust), `d0ec1b4` (холостой
        тест), `5d3a991` (фронт).
        - **Блокирующая находка: тест «пароли не попадают в кэш мутаций» был холостым.**
          Дамп снимался сразу после `await`, когда заявки гейта ещё `pending` и `data` у
          КАЖДОЙ мутации `undefined`, — ассерт смотрел в состояние, где пароля не может
          быть в принципе. Заявка, резолвящаяся полным отчётом, оставляла все 7 тестов
          зелёными. Теперь тест дожидается оседания заявок и отдельно утверждает, что
          данные у них есть. Файл перечитан целиком: других ассертов «в невозможный
          момент» нет.
        - Залипший `running` больше не врёт: `previous_status` различает `running` и
          `done`, а `running` старше `BULK_RUN_STALE_AFTER_HOURS` (24 ч) считается
          осиротевшим и пропускает новый прогон. `done` не протухает никогда.
        - Итог прогона — `BulkProvisionReportModal` вместо тоста (2200 мс под модалками с
          паролями = не увиденный обрыв), со списком id незапущенных доменов и ключом
          прогона.
        - `Modal` получил `closeOnBackdrop`; у `ProvisionResultModal` и
          `FastPanelCredsModal` он `false` — их `onClose` уничтожает единственную копию
          секрета. Очередь показов считает «1 of 20».
        - Комментарий про идемпотентность повтора назвал `create_ftp_account` как
          неидемпотентный шаг (ссылка на фазу 3b).
        - `BULK_SITE_ONLY`/`BULK_WITH_DB` вместо двух позиционных `false`: прод-проводку
          команды не покрывает ни один тест, и подмена `site_only` на `true` отменяла
          создание FTP-аккаунтов молча.
        - Мелочи: дубликаты `ids=1,1,1` схлопываются; заявка гейта помечена
          `bulkGateClaim`; провал на середине набора отпускает уже поставленные заявки;
          `showToast` гасит предыдущий таймер.
        - Тесты: cargo **126 passed** (было 120), vitest **255 passed / 32 files** (было
          248). 18 новых мутационных проверок — журнал ниже.
        - **Остаток** (уточнён по итогам spec-ревью):
          - Гейт держится в `MutationCache`, и его настоящая граница — не «одно окно», а
            **время жизни страницы**: F5 или перезагрузка вебвью стирает кэш мутаций, а
            Rust-задача продолжает работать — ⚙ на провижинящемся домене снова активна.
            Это свойство гейта из спринта 3, не регресс этой фазы, но формулировка «внутри
            одного окна» его прикрывала. Второй процесс десктопа кэш тоже не видит.
          - Пересекающиеся bulk-наборы **внутри одной страницы** новый гейт блокирует
            (заявки подоменные). Не защищает от них только `bulk_runs`, который ключуется
            на наборе id целиком: `{1,2,3}` и `{3,4}` — разные ключи.
          - Нечисловые id в ссылке (`sdmp://bulk-provision?ids=abc`) гейт **не занимают
            вовсе**: он ключуется числовым `domainId`, тем же, что кладёт в `variables`
            страница. Оговорено комментарием в `api/domains.ts` (рядом с `Number.isInteger`).
            Такой домен отобьёт Rust («domain not in local cache»), и он попадёт в отчёт
            как `failed`, но параллельный запуск по нему ничем не остановлен.
          - Сквозная защита требует подоменного гарда в Rust (таблица «домен занят» в
            SQLCipher-кэше) — отдельная задача, она же закроет долг №7 («три формы гейта
            свести к одной»).
          - Повтор массового прогона безопасен только по хвосту `skipped`:
            `create_ftp_account` зовётся безусловно, `ftp_exists` не существует, логин
            детерминирован, пароль генерируется заново. Закрывает фаза 3b (долг №5).
          - Прод-проводка `provision_bulk` (десять строк между командой и
            `run_bulk_provision`) тестами не покрыта — нет харнесса под Tauri `State`.
            Защита сегодня — именованные константы и читаемость диффа.
          - Сброс таймера тоста тестом не покрыт: проверка требует фейковых таймеров
            поверх RTL, а цена бага — тост, живущий чужой остаток времени. Итог bulk от
            этого больше не зависит — он теперь модалка.
- [ ] **4. Видимость упавшего provision** — писать `last_provision_error` до раннего возврата,
      показать в UI (№4).
- [ ] **5. Честность ZK-тестов** — проверка по БД вместо `secret not in r.text`, позитивный
      контроль URL для кросс-юзерных 404, мутационная проверка (№6).

## Чек-лист ручной приёмки (сквозной сценарий)

Завести сервер с SSH-паролем → Install FastPanel → завести домен → CF DNS-редактор
(A-запись) → Set NS → Provision с `withDb` → повтор (гарды `site_exists`/`ssl_exists`/
«уже установлено») → упавший provision виден как «Last error». В БД плейнтекст-секретов нет,
`*_blob_id` заполнены, blob'ы расшифровываются.

## Что построено к паузе (T1 + T2)

- `frontend/src/lib/secretBlob.ts` — `putSecretBlob({plaintext, blobKind, existingBlobId})` и
  `deleteSecretBlob`. `blobKind` — union `BlobKind`, `existingBlobId` обязателен и nullable:
  перепутать соседние `string`-аргументы было нельзя допустить, иначе пароль уехал бы в
  `blob_storage.blob_kind` и в `audit_log.metadata`.
- `frontend/src/hooks/useSecretSave.ts` — общая церемония для всех форм с секретом: плейнтекст
  живёт в хуке (не в стейте страницы), порядок «блоб → сущность», единый текст ошибки,
  `saving` держится по промису `mutateAsync`. Фаза 1b обязана переиспользовать его, а не
  копировать код форм.
- `frontend/src/components/DesktopOnlyNote.tsx` — один вид у фразы `desktopOnly(...)`.
- `frontend/src/test/secretBlobKit.ts` — общие тестовые хелперы (`setTauri`, `putBlobArgs`).
- SSH-пароль проведён в `pages/Servers.tsx` (вкладка install) и `pages/ServerDetail.tsx`;
  `ssh_password` удалён из `ServerCreate`/`ServerUpdate` — теперь это ошибка типа.

Тесты на паузе: vitest **193 passed** (было 165 на старте спринта); pytest и cargo не
трогались с baseline.

### Новый долг, найденный по дороге (в cleanup-спринт)

- **Версионирования API нет** (префикс `/api` без версии, согласования версии клиента нет,
  CI нет), а `extra="forbid"` с фазы 2 превращает любое переименование поля схемы в жёсткий
  разрыв: старый клиент получит 422 вместо игнорирования лишнего поля. Сегодня риска нет —
  старых сборок в поле не существует, единственный внепользовательский клиент
  (`ServerWriteBack` десктопа) укладывается в `ServerUpdate`. С первой отгрузки десктопа
  нужен путь миграции (версия в префиксе либо согласование версии клиента). Не всплыть бы
  этому на launch prep.
- 285 тестовых пользователей `swp-*`/`forbid-*` в общей dev-БД, накопленных до фикстуры
  уборки, так и лежат — новые прогоны их больше не плодят, но старые никто не удалял.
  Снести одним DELETE по префиксам, когда будет с кем сверить, что это точно мусор.
- `npm run build` (`tsc && vite build`) **уже красный** от 51 предсуществующей ошибки
  (`Cloudflare.tsx` 14, `Settings.tsx` 9, `Servers.tsx` 9, `lib/crypto.ts` 9,
  `ServerDetail.tsx` 7, `Notifications.tsx` 2, `UnlockModal.tsx` 1 — в основном `TS7006`).
  Следствие: типовые гарды не роняют сборку, потому что ронять нечего. Нужен зелёный
  `tsc` и отдельный скрипт `typecheck`.
- **Bulk-import серверов грузит CSV с колонкой `ssh_password` на сервер** — плейнтекст на
  проводе, и фаза 2 его не поймает: это multipart, а не JSON-схема. Чтобы стать ZK, разбор
  CSV должен уехать на клиент. Отдельная задача, в объём спринта 4 не входит.
- `saveAll` возвращает `Partial<Record<K, string>>` даже для объявленных ключей, поэтому
  `api_key_blob_id: blobIds.apiKey` имеет тип `string | undefined`, `JSON.stringify`
  выбросит его — и получится `*_blob_id = NULL` и 200 OK без единой ошибки типа. Защищает
  только комментарий. Обобщить типизацию, чтобы объявленные ключи приходили обязательными.
- Второй «опциональный одиночный секрет» (`EditCfAccountModal` после `ServerDetail`)
  переехал на многополевой `useMultiSecretSave`. Появится третий — заводить
  `save({ target: SecretTarget | null })` у одиночного хука, а не бежать в многополевой.
- Заблокированный на время сохранения `Cancel` — единственный выход из модалки, и `Esc` у
  `Modal` нет: зависший `vault_put_blob` запирает пользователя в окне. У `AddCfAccountModal`
  канал для провала уже есть (`onStatus`) — можно вернуть живой Cancel.
- `api_token_masked` показывает хвост **blob id**, а не токена, под подписью «Token»
  (`Cloudflare.tsx:184`). Убрать или переименовать.
- `registrar_service.py:57-67` применяет `api_*_blob_id` только при `not None`, а
  `exclude_unset` делает явный `null` в PUT неотличимым от отсутствия поля → снять секрет
  регистратора через PUT нельзя. Всплывёт в 1b-2, если форме понадобится «убрать секрет».
- Во `frontend/` нет eslint вообще (ни `eslint.config.*`, ни `.eslintrc*`, ни секции в
  `package.json`) — значит `no-floating-promises` и подобные страховки не работают. Отсюда
  рантайм-гвард на вложенный `save` вместо расчёта на линтер.
- Мёртвые deep link'и, предсуществующие: `add-server`, `ssh-test`, `refresh-metrics`,
  `sync-domains`, `delete-server` в `ServerDetail.tsx`/`Servers.tsx` и вся
  `BulkActionToolbar.tsx` — хосты, которых `parseDeepLinkAction` не знает: ссылка ведёт в
  `{handled:false}` и только тостит.

## Журнал мутационных проверок (фаза 3a)

Rust (`cargo test --lib commands::provision`):

| Что сломано | Что покраснело | Вернул |
|---|---|---|
| На провале возвращаем `items: Vec::new()` (как было — `return Err`) | `partial_failure_keeps_the_results…` | да |
| Убран проход, помечающий хвост `Skipped` | `partial_failure_keeps_the_results…` | да |
| Ранний возврат отдаёт `BulkRunStatus::Ok` вместо `AlreadyRan` | `a_run_that_already_happened…` | да |
| Сбой записи финального статуса снова `return Err` | `a_failed_status_write_still_returns_the_passwords` | да |
| Успешный домен кладётся как `Skipped`, а не `Done` | 3 теста разом | да |
| `#[serde(tag = "outcome")]` → `untagged` | `bulk_report_serializes_with_a_readable_outcome_tag` | да |

Vitest:

| Что сломано | Что покраснело | Вернул |
|---|---|---|
| Deep link выбрасывает отчёт (`return { handled: true }`) | «показывает результат каждого домена bulk-провижининга» | да |
| Воркспейс кладёт в очередь только `results[0]` | то же | да |
| `claimProvisionGate` не вызывается | «занимает гейт по каждому домену», «гасит ⚙» | да |
| Снята проверка `alreadyProvisioning` | «не стартует, пока уже идёт одиночный provision» | да |
| `release()` не вызывается в `finally` | 3 теста про отпускание гейта | да |
| `already_ran` не распознаётся в `summarizeBulkProvision` | «говорит словами, что прогон уже был» | да |
| `skipped` схлопнут в `failed` при разборе отчёта | 2 теста | да |
| `console.log(res)` в `runBulkProvisionDomains` | «не пишет пароли ни в кэш, ни в storage, ни в консоль» | да |
| `localStorage.setItem(..., JSON.stringify(res))` | то же | да |
| Отчёт возвращён из `mutationFn` (попал в `data` MutationCache) | то же | да |
| Тост дописывает пароль первого домена | «тост не содержит паролей» + тест воркспейса | да |

Тест на неутечку проверен отдельно тремя мутациями (консоль, storage, кэш мутаций) —
**но ветка про кэш мутаций оказалась холостой, см. журнал 3a-fix ниже.**

## Журнал мутационных проверок (3a-fix, правки по ревью качества)

Rust:

| Что сломано | Что покраснело | Вернул |
|---|---|---|
| Осиротевший `running` снова блокирует навсегда | `an_orphaned_running_row_stops_blocking_after_a_day` | да |
| Протухание распространено и на `done` | `a_finished_run_keeps_blocking_no_matter_how_old_it_is` | да |
| `previous_status: None` — `running` и `done` неразличимы | 2 теста | да |
| Неразобранная дата считается протухшей | `an_unreadable_start_time_is_never_treated_as_stale` | да |
| Порог протухания обнулён | 2 теста | да |
| `created_at` не читается из БД | 2 теста (в т.ч. `sync::cache`) | да |
| `BULK_SITE_ONLY = true` | `bulk_runs_a_full_provision_and_never_a_site_only_one` | да |

Vitest:

| Что сломано | Что покраснело | Вернул |
|---|---|---|
| **Заявка гейта резолвится полным отчётом** (мутация ревьюера) | «не пишет пароли…» — **до фикса теста была зелёной**, после фикса красная | да |
| Итог bulk снова тостом | «показывает результат каждого домена…» | да |
| Итог лезет поверх модалок с паролями | то же | да |
| Хвост показан только счётчиком, без id | то же | да |
| Ключ прогона не показан на экране итога | то же | да |
| Счётчик «1 of N» не показывается | 2 теста | да |
| `closeOnBackdrop` снят с `ProvisionResultModal` | «не закрывается кликом в затемнение» | да |
| Счёт серии в очереди не обнуляется | «считает место в серии…» | да |
| `running` и `done` говорят одно и то же | «про незакрытый прогон НЕ утверждает…» | да |
| Ключ не доезжает из ответа | 2 теста | да |
| Дубликаты id не схлопываются | «схлопывает дубликаты id из ссылки» | да |
| Провал на середине набора не отпускает заявки | «не оставляет висеть заявки…» | да |
| `closeOnBackdrop` снят с `FastPanelCredsModal` | «не закрывается кликом в затемнение — пароль панели» | да |
| Компонент итога лезет в `results[].result.ftp.ftp_password` | **`tsc`**: TS2339 в `BulkProvisionReportModal.tsx` (45 → 46 ошибок; вне мутации мои файлы чисты) | да |
| Итог снова не знает про `fpQueue` | «не накрывает итогом креды FastPanel» | да |

Тест из последней строки специально ставит прогон БЕЗ единого удавшегося домена: иначе
проверку закрывал бы `!provisionQueue.current`, и она была бы холостой — та же ошибка, что
уже была поймана в тесте на кэш мутаций.

## Итог

_Заполняется по завершении спринта._
