# План: Вкладка Domains — сроки, сортировка, статистика

**Дата:** 2026-08-11
**Источник:** `task7.md`, план №2 роадмапа.
**Связь с бизнесом:** целевой пользователь ведёт сотни доменов. Модель хранит
сроки истечения домена и сертификата, но в списке их нет — то есть просрочку
пользователь узнаёт от клиента, а не от продукта. Это самый болезненный пробел
вкладки.

## Контекст (проверено кодом)

Бэкенд менять не нужно: `DomainResponse` (`backend/app/schemas/domain.py`) уже
отдаёт `expiry_date`, `purchase_date`, `ssl_expires_at`, `ssl_issuer`,
`php_version`, `db_name`, `db_user`. TS-интерфейс `Domain`
(`frontend/src/api/domains.ts:9`) их тоже уже объявляет. Всё, чего не хватает, —
показ.

Рассинхрон, подтверждённый кодом: `DomainStatus` (`backend/app/core/constants.py:23`)
содержит `ns_ok`, но его нет **в двух местах** фронта — в фильтре статуса
(`Domains.tsx:551–560`) и в карте `StatusBadge` (`components/StatusBadge.tsx:5`).
Домен в статусе `ns_ok` сегодня не отфильтровать, а бейдж ему рисуется серым
фолбэком.

**Зависимость:** делать после плана
`2026-08-11-domains-chistka-sloya-ispolneniya.md` (в нём со страницы уходит
мёртвый код, который иначе пришлось бы править дважды).

## Acceptance criteria

- [x] В таблице есть колонки **Expires** (домен) и **SSL** со сроком.
- [x] Порог «скоро истекает» живёт в ОДНОЙ чистой функции в отдельном модуле
      (`frontend/src/lib/domainExpiry.ts`), по образцу `lib/serverStatus.ts`,
      а не размазан по JSX. Функция покрыта юнит-тестом.
- [x] Отсутствие даты рисуется как «не знаем» (`—`), а не как «всё хорошо».
      Правило продукта №6: незнание — отдельное состояние, не зелёный бейдж.
- [x] Сортировка по клику на заголовки: domain, expires, ssl expires, status,
      added. Повторный клик — обратный порядок. Направление видно в заголовке.
- [x] Карточки статистики показывают срез по lifecycle-`status`, а не только по
      `ns_status`.
- [x] `ns_ok` есть и в фильтре статуса, и в карте `StatusBadge`.
- [x] `overview` в `DomainDetailModal` обогащён: php_version, db_name, db_user,
      ssl_issuer, ssl_expires_at, expiry_date, site_user/site_path, ftp_user.
- [x] `npx tsc --noEmit` и `npm test` во `frontend` зелёные.

## Edge cases

- **Даты нет** (`expiry_date === null`) — `—`, нейтрально; НЕ «ок» и НЕ «просрочен».
- **Дата битая** (`new Date(x)` → `NaN`) — тот же нейтральный исход, не крэш.
- **Сортировка при `null`** — записи без даты всегда в конце, при любом
  направлении: иначе «сортировка по сроку» первым делом показывает тех, про кого
  срок неизвестен.
- **Сотни строк** — сортировка в `useMemo`, не в рендере строки.
- **Время** — одно чтение часов на рендер (`const now = Date.now()`), как уже
  сделано на этой странице для статусов серверов. Разные «сейчас» в соседних
  ячейках — это разъезжающиеся ответы на один вопрос.
- **Часовые пояса** — `expiry_date` приходит как `date` (без времени),
  `ssl_expires_at` — как `datetime`. Считать в днях, не в часах.
- **Сортировка + фильтры + `focusDomainId`** — порядок применения: фильтр,
  затем сортировка; фокус-режим не должен ломать сортировку.

## Фазы

### Фаза 1 — `lib/domainExpiry.ts`: единый источник правды о сроках  `[x]`

- Новый модуль с чистыми функциями и явным параметром `now: number`
  (по образцу `lib/serverStatus.ts` — там `now` параметром именно ради
  одинакового «сейчас» на всём рендере и ради тестируемости).
- Состояния: `unknown` (даты нет / дата битая), `expired`, `soon`, `ok`.
- Порог `soon` — 30 дней, вынести именованной константой с комментарием
  «почему столько» (продление домена и перевыпуск сертификата — обе операции
  с запасом укладываются в месяц).
- Функции: определение состояния по ISO-дате и форматирование подписи
  (например `in 12 days` / `expired 3 days ago` / `—`).
- Юнит-тест `frontend/src/lib/domainExpiry.test.ts`: все четыре состояния,
  граница ровно на пороге, `null`, мусорная строка.

### Фаза 2 — Колонки Expires и SSL в таблице  `[x]`

Файл: `frontend/src/pages/Domains.tsx` (+ `DomainUI` расширить полями
`expiry_date`, `ssl_expires_at`).

- Колонка **Expires** — дата + подпись состояния, цвет по состоянию
  (жёлтый — `soon`, красный — `expired`, приглушённый — `unknown`).
- Колонка **SSL** — существующий бейдж статуса дополнить сроком из
  `ssl_expires_at` (тем же модулем). Не заводить вторую колонку под SSL-срок,
  если он логично встаёт в существующую.
- Цвета брать из уже используемых на странице значений (страница пока
  хардкодит hex; токенизация — общеприложенческая инициатива, не эта задача).

### Фаза 3 — Сортировка по колонкам  `[x]`

- Состояние `sort: { key, dir }` на странице; клик по `<Th>` переключает.
- Ключи: `domain`, `expiry_date`, `ssl_expires_at`, `status`, `created`.
- Индикатор направления (↑/↓) в активном заголовке.
- `null`-даты всегда в конце независимо от направления.
- Сортировка применяется после фильтрации, в `useMemo`.
- `<Th>` становится кликабельным только для сортируемых колонок — у чекбокса и
  колонки действий сортировки нет.

### Фаза 4 — Статистика и фикс рассинхрона `ns_ok`  `[x]`

- Карточки статистики: добавить срез по lifecycle-`status`
  (active / provisioning / failed) рядом с NS-срезом. Реализация — на выбор
  исполнителя: либо второй ряд карточек, либо переключатель NS ↔ lifecycle;
  выбранное решение объяснить комментарием в коде.
- Добавить `ns_ok` в `<Sel>` фильтра статуса (`Domains.tsx:551`).
- Добавить `ns_ok` в карту `StatusBadge` (`components/StatusBadge.tsx`) —
  вариант `green` или `blue` по смыслу «NS проставлены».
- Проверить, что список статусов фильтра совпадает с `DomainStatus` целиком.

### Фаза 5 — Обогатить `overview` в `DomainDetailModal`  `[x]`

- Добавить read-only поля: `expiry_date`, `ssl_status` + `ssl_expires_at` +
  `ssl_issuer`, `php_version`, `db_name`, `db_user`, `site_user`, `site_path`,
  `ftp_user`.
- Сроки — через тот же `lib/domainExpiry.ts`.
- Пустое поле рисовать как `—`; ничего не выдумывать за модель.
- Паролей здесь нет и быть не может (сервер их не знает) — не заводить полей
  под них даже пустыми.

### Фаза 6 — Верификация  `[x]`

- `cd frontend && npx tsc --noEmit`
- `cd frontend && npm test`
- Тест на сортировку и на то, что домены без даты уходят в конец.

## Итог

**Реализован целиком: да** (фазы 1–6). Коммит `629775e` в ветке
`feat/domains-tab-overhaul`.

Числа: `npx tsc --noEmit` чист; `npm test` — 68 файлов / 664 теста зелёные
(база ветки была 65 / 635). Изменены `frontend/src/pages/Domains.tsx`,
`components/DomainDetailModal.tsx`, `components/StatusBadge.tsx`,
`api/cloudflare.ts` (только комментарий); добавлены `lib/domainExpiry.ts`,
`lib/domainStatus.ts` и три теста — `lib/domainExpiry.test.ts` (11),
`pages/Domains.expiry.test.tsx` (13), `components/DomainDetailModal.overview.test.tsx` (5).

### Что решено по ходу

- **Порядок по умолчанию — по имени, по возрастанию.** Список на сотни строк —
  это в первую очередь поверхность поиска; порядок с бэкенда (по id) для
  пользователя означает случайный.
- **Статус сортируется по лестнице жизненного цикла, а не по алфавиту.**
  Алфавит («active, failed, new, ns_ok…») не отвечает на вопрос «что не
  доделано». Порядок лестницы совпадает с `DomainStatus` бэкенда; убывание
  поднимает `failed` наверх — это и есть второй клик по заголовку.
- **Статистика — вторым рядом карточек, не переключателем.** Переключатель
  прячет половину состояния за клик, а страница как раз и отвечает на вопрос
  «всё ли в порядке». «In progress» считается ОСТАТКОМ (всё, что не new/active/
  failed), поэтому ряд сходится с Total при любом новом статусе бэкенда;
  перечисление промежуточных статусов молча теряло бы новый — ровно так и
  потерялся `ns_ok`.
- **Лестница статусов вынесена в `lib/domainStatus.ts`.** Рассинхрон `ns_ok`
  был не опечаткой, а следствием того, что список статусов жил на фронте в двух
  копиях (карта `StatusBadge` и пункты фильтра). Теперь обе строятся из одного
  массива, и третьим потребителем стала сортировка.
- **`useZoneDetails` дом НЕ получил, долг остаётся.** Статус зоны просился бы на
  вкладку `overview`, но она открыта всегда — то есть `cf_list_zones` (аккаунт
  по 50 зон, только десктоп) уходил бы на каждое открытие любой карточки ради
  одного слова, и в вебе поле всё равно осталось бы пустым. Вкладка `ns` платит
  эту цену потому, что без списка NS ей нечего показывать вовсе. Настоящий
  потребитель — план №3 (автопривязка по имени зоны). Решение записано
  комментарием в `api/cloudflare.ts` рядом с хуком.
- **Имена сервера/регистратора на карточке домена остались id.** За именами
  пришлось бы тянуть в компонент три списка или три новых пропа — это работа
  плана №4. Зафиксировано тестом, чтобы не поменялось молча.

### Правки в уже написанной фазе 1 (она была не проверена)

- **Off-by-one в `formatExpiry` на целых сутках.** Было `elapsed = -days - 1` от
  `Math.floor` отрицательной разности: `floor` уводит ОТ нуля, поэтому «ровно
  сутки назад» и «час назад» давали одно число, «expired 1 day ago» было
  невыразимо вовсе, а любой ровный день назывался вчерашним. У `expiry_date`
  (тип `date`, без времени) ровные сутки — единственный случай, который вообще
  бывает. Прошедшее считается отдельным `floor` от положительной разности; тест
  на границу добавлен (старый её обходил, беря смещения с часом сверху).
- **Добавлен `expiryTs`** — разбор даты для сортировки. Без него компаратор
  звал бы `new Date(x).getTime()` сам, и одна битая дата (`NaN` ложен во всех
  сравнениях) делала бы произвольным весь порядок.
- Остальное проверено и оставлено как есть: границы `expiryState` (`days < 0` →
  `expired`, `<= 30` → `soon`), уравнивание битой даты с отсутствующей, четыре
  хекса `expiryTextColor` — они точно совпадают с `Badge` red/yellow, `DIM_TEXT`
  и обычным тёмным текстом форм страницы. Комментарий про источник цветов
  уточнён (в нём не был назван четвёртый).

### Что осталось (долг)

- `Domains.tsx` вырос до ~1000 строк — разбор на компоненты остаётся планом №4.
  Карточки статистики и фильтр по-прежнему живут в теле страницы; сортировка
  вынесена отдельной функцией (`sortDomains`) и переезжает вместе с таблицей.
- Колонка «Added» рисуется цветом `DIM_TEXT`, которым на этой же странице
  рисуется «мы не знаем». Дата заведения известна всегда — цвет достался ей от
  прежней вёрстки; трогать не стал, это не про сроки.
- `npm test` в полном прогоне изредка роняет `Cloudflare.collapse.test.tsx`
  таймаутом `findByText` (7 зелёных прогонов из 9; в одиночку файл зелёный
  4/4, кода Cloudflare эта работа не касалась). Это известный долг Д12 плана
  `2026-08-10-cloudflare-доработки.md` — сюита флакует таймаутами и на базе
  ветки.
