# plans/ — технические планы реализации

Это место для технических планов реализации функций.

> Исторические планы миграции SDMP на zero-knowledge — спринты 1–4 — лежат здесь же
> (файлы `2026-08-0*-sprint*.md`), с итоговыми блоками и остатком долга.
> В корне проекта из миграции остался только `stage5.md` (launch prep — единственная
> невыполненная стадия). Общая спека `plan.md`, стадии `stage0.md … stage3.md` и
> корневые копии спринтов **удалены** как исполненные: `stage0…3` в `6428806`,
> остальное — позже. На диске их нет, ищи в истории git. Планы ниже ссылаются на них
> строкой «Источник: …» — это запись происхождения, а не указатель на живой файл;
> так же читай упоминания `task5.md`/`task6.md`/`task7.md`. Роадмап вкладки
> Domains (`task7.md`) исполнен целиком и **удалён из корня** тем же правилом:
> он живёт в истории git начиная с `1460480`, а фактический итог и остаток долга
> — в четырёх планах `2026-08-11-*` ниже. Тем же правилом убран `task8.md`
> (Cloudflare ↔ Domains: живой матч и full-setup): исполнен целиком, живёт в истории git,
> итог и долг — в двух планах `2026-08-13-*`. Новые функции оформляй здесь.

## Текущие планы

| План | Статус | Что осталось |
|---|---|---|
| `2026-08-02-sprint0-stabilizaciya.md` | выполнен | — |
| `2026-08-02-sprint1-yadro-ispolneniya.md` | выполнен | часть Tauri-команд без точки входа в UI |
| `2026-08-02-sprint2-bezopasnost-zk.md` | выполнен | 6 пунктов переноса — см. итоговый блок |
| `2026-08-03-recovery-proof-of-phrase.md` | фазы 1–4, 6 | ручной сквозной прогон (фаза 5) |
| `2026-08-03-sprint3-ui-reachability-writeback.md` | выполнен | 12 пунктов долга — см. итоговый блок |
| `2026-08-04-sprint4-secret-write-path.md` | выполнен | долг №7–12 вынесен в cleanup-спринт |
| `2026-08-04-cleanup-debt-9-12.md` | **выполнен целиком** | соседние долги — см. итоговый блок |
| `2026-08-06-server-monitoring-6h.md` | **выполнен целиком** (части I–IV) | долг — в итоговых блоках: эфемерная БД под тесты (в общей dev-БД 2563 мусорных пользователя, и триггер на старте воркера обходит их все), валидация `ip_address`, ослабший по смыслу `FAILURE_THRESHOLD` |
| `2026-08-07-versionnost-sborki.md` | **выполнен целиком** | — |
| `2026-08-10-редактирование-инфо-сервера.md` | **выполнен целиком** (фазы 1–4) | долг — в итоговом блоке: IPv6-сервер не может подключить FastPanel (три парные регулярки во фронте, бэкенде и Rust не знают bracketed-IPv6); импорт не принимает колонку `os`; два карандаша на одно переименование |
| `2026-08-10-pole-os-v-forme-servera.md` | **выполнен целиком** (фазы 1–2) | долг — в итоговом блоке: bulk import заводит серверы без `os`, а дописать `os` потом негде; `update_command` — else-fallback, а не whitelist |
| `2026-08-09-server-tab-improvements.md` | **выполнен целиком** (фазы 1–3) | долг — в итоговом блоке: карточка «FastPanel Access» врёт про порт/версию/протокол у подключённой панели; у подключённой панели нет ни правки кред, ни отмены; `RevealSecret` пароля панели недостижим в десктопе |
| `2026-08-10-cloudflare-сворачивание-и-счётчик.md` | **выполнен целиком** (фазы 1–4) | вне объёма изначально: автопривязка доменов к Cloudflare по имени зоны |
| `2026-08-10-cloudflare-доработки.md` | **выполнен целиком** (фазы 1–4) | 2.3 отложена осознанно (счётчик DNS-записей потребовал бы `cf_list_dns_records` на каждую зону). Долг Д1–Д13 в итоговом блоке; ценнее прочего — Д7 (разбить `pages/Cloudflare.tsx`, 1278 строк: без этого у экспортированных чистых функций нет юнит-теста) и Д11 (общий тестовый кит: 450 строк дословного дубля на семь файлов). Отдельно Д12 — `npm test` флакует таймаутами и на базе ветки, то есть критерий «сюита зелёная» сейчас непроверяем |
| `2026-08-11-domains-chistka-sloya-ispolneniya.md` | **выполнен целиком** (фазы 1–5) | план №1 роадмапа Domains из `task7.md`. Отложено осознанно (см. «Явно откладываем»): возврат гранулярных операций по домену (Create Site / Create DB / Request SSL / Refresh SSL / nginx-override) и Full Setup — обе требуют новых Tauri-команд и SSH-логики, обе — отдельные фичи со своими планами. **Живой долг:** гонка «переназначение сервера во время массового прогона» — Rust читает `server_id` лениво, на каждой итерации (записана без правки кода); `useZoneDetails` рабочий, но без производственных вызывающих — поручение «если план №2 им не воспользуется, удалить там же» **отозвано** (план №2 отказался осознанно: `overview` — всегда открытая вкладка, цена — `cf_list_zones` на каждое открытие карточки; план №3 им стать не мог — ему нужен весь список зон по имени, а хук выбирает одну по id). Посылка аудита `task7.md` про «`apiPost` — чистый axios» была неверна — **исправлена в самом `task7.md`** (вывод про 404 верен: `api_request` проксирует в тот же REST API) |
| `2026-08-11-domains-sroki-sortirovka-statistika.md` | **выполнен целиком** (фазы 1–6) | план №2 роадмапа Domains. Рассинхрон `ns_ok` закрыт структурно: лестница статусов вынесена в `lib/domainStatus.ts` и питает фильтр, бейдж и сортировку. **Закрыто планом №4:** рост `Domains.tsx` до 1077 строк (сегодня 399) и все четыре «заготовки» (`StatCard`, `Th` → `PlainTh`/`SortableTh` с обязательным `label`, `dateOf` со `switch`+`never`, `StatusBadge` → `DomainStatusBadge`). **Живой долг:** порог «скоро истекает» app-wide всё ещё в двух местах с разной семантикой — свой `SSL_EXPIRING_SOON_DAYS`/`isSslExpiringSoon` в `pages/dashboardData.ts` (без `floor`, просроченный сертификат считает истекающим); колонка «Added» рисуется цветом «мы не знаем»; `process.env.TZ` в `Domains.expiry.test.tsx` не возвращается обратно; императивная подсветка строки под курсором — обещание «лечится `:hover` при разборе страницы» **снято**, план №4 починку отклонил с обоснованием (такой же ховер на шести страницах), долг переехал в общефронтовую работу по `design-brief.md`; `useZoneDetails` осознанно НЕ получил дом на карточке домена — см. строку выше |
| `2026-08-11-avtoprivyazka-cloudflare-po-zone.md` | **выполнен целиком** (фазы 1–5) | план №3 роадмапа Domains; закрывает долг из `task5.md`. Правило матчинга — чистый `lib/cfZoneMatch.ts`, прогон — `api/cfAutoBind.ts` (только десктоп: зоны читает `cf_list_zones`). Отклонения от критерия (оба обоснованы в итоговом блоке): итог показывается баннером страницы, а тостом воркспейса — только если страница уже размонтирована; текст английский; при «нет активных аккаунтов» автопривязка молчит, а кнопка отвечает. Правило «перезаписи не происходит» проверяется и по свежему кэшу перед каждой записью — снимок за десятки секунд прогона устаревает, а «Assign CF» во время него намеренно не заблокирован. **Закрыто планом №4:** гейт «один прогон за раз» переживает навигацию (M4 — заявка в `MutationCache` под `CF_BIND_KEY`); рост `Domains.tsx` до 1291 строки (сегодня 399); заготовки (а), (б), (г). Заготовка (в) — свёртка четырёх «доставок наверх» в один колбэк — **отклонена с обоснованием, а не отложена**: union-тип полезной нагрузки снял бы с компилятора требование каждого из четырёх обработчиков, а именно оно делает потерю пароля FTP/БД невыразимой; вдобавок после разбивки каждый проп зовётся ровно из одного места. **Живой долг:** полной гарантии «не перезаписать» на клиенте нет — окно сужено до одного круга `GET /domains`, закрыть его может только условная запись на бэкенде (`PUT /domains/{id}` безусловен); поддомен к родительской зоне не привязывается (осознанно); домен с аккаунтом, но без `zone_id`, не трогается; файловый импорт не подключён (в ответе нет созданных строк); `normalizeZoneName` экспортирован только ради своего теста |
| `2026-08-11-domains-razbivka-god-komponenta.md` | **выполнен целиком** (фазы 1–6) | план №4; чистая структура — ни одного изменения в том, что видит пользователь, кроме M4 (ниже). `pages/Domains.tsx` — 399 строк вместо 1291, 25 модулей в `components/domains/` и `hooks/`. Сюита прошла без единой правки ассерта (в тестах изменён один путь импорта). Закрыт долг планов №2 и №3: `interface StatCard` вместо кортежа через `as`, `Th` → `PlainTh`/`SortableTh` с обязательным `label`, `dateOf` со `switch`+`never`, `StatusBadge` → `DomainStatusBadge`, `useLiveOrAway`, `useCloudflareBind`; **M4** — гейт «один прогон привязки за раз» переехал в `MutationCache` и переживает уход со страницы (единственное намеренное изменение поведения, есть падающий на прежней реализации тест). Отклонено с обоснованием в итоге: свёртка четырёх «доставок наверх» в один колбэк (потеряла бы требование компилятора на каждый) и перевод ховера строки на CSS (императивный ховер живёт ещё на шести страницах — правится общефронтово вместе с `design-brief.md`). По ревью исправлено второе изменение поведения, которого план не разрешал: сортировка, уехавшая внутрь `DomainTable`, не переживала подмену таблицы (в том числе на экран ошибки от упавшего ФОНОВОГО рефетча) — порядок вернулся на страницу в `useDomainSort`, есть тест на срок жизни. По второму ревью закрыто: строка перестала перерисовываться на каждую букву (мемоизация была невозможна по построению — `Date.now()` на каждый рендер уезжал пропсом в строку; теперь `useNow` с тиком в минуту, `React.memo` и стабильные колбэки, есть тест на число рендеров), появились прямые юнит-тесты правил порядка, достроены «вместо таблицы» (`DomainsLoading`, `BulkProvisionErrorBanner`). Долг — в итоговом блоке: транзитные пропсы `DomainTable` (10 из 15 — для строки), перевёрнутые слои (`hooks/` импортирует из `components/`, включая ТИП `DomainFilterControls` у потребителя) и стоимость на строку (немемоизированные счётчики в `DomainStats`, три `find` на строку, инвариант «выделено всё» в двух местах). **Там же — сводный долг ВЕТКИ `feat/domains-tab-overhaul`** (план №4 был последним из четырёх): живой долг всех четырёх планов одной таблицей; исправленный уборкой зелёный «✓» на прогоне привязки, не привязавшем ничего (`summarizeCfBind` не считал `none` неполным успехом); документы вне вкладки, которые ветка сделала неверными (`docs/ARCHITECTURE.md`, `docs/CURRENT_STATUS.md`, `design-brief.md` называют удалённые `BulkSetupWizard`/`StatusBadge`/`MultiTaskProgressModal` и пятивкладочный `DomainDetailModal`) — правка вынесена в отдельное решение; довод за приоритет Д11 (пять новых полностраничных сюит `Domains.*`, ~1800 строк, увеличили поверхность Д12) |

| `2026-08-13-domains-cloudflare-zhivoy-match.md` | **выполнен целиком** (фазы 1–3) | Кейс 1 из `task8.md`: колонка Cloudflare в строке показывает живой матч подсказкой (курсив, отличимый от сохранённой привязки), кнопка «Синхронизировать с Cloudflare» гоняет привязку по всему списку, карточка домена лишилась вкладок — аккаунт, зона и делегирование NS читаются вместе. Ревью вскрыло три вещи, которых спека не требовала: в react-query v5 фоновый провал **сохраняет `data` и ставит `error`**, поэтому аккаунт с отозванным токеном молча считался прочитанным; бейдж делегирования у Namecheap не мог быть ничем, кроме серого UNKNOWN (`domains.getList` nameservers не отдаёт, а `get_nameservers` был реализован, но наружу не выставлен и звал не тот метод API); **API-ключ Namecheap уезжал в текст ошибки на экран** — `reqwest` дописывает URL в `Display`, а ключ у Namecheap query-параметр. Живой долг — в итоговом блоке: смешанный язык (две русские кнопки посреди английского UI), подсказка неотличима от факта для скринридера, автозапись `cloudflare_zone_id` без клика пользователя, принятый регресс со скоупом отказа после слияния вкладок, `clip`/`errorText` живут в модуле прогона привязки, а импортируют их три презентационных компонента |
| `2026-08-16-fastpanel-database-vs-databases.md` | **не начат** | Follow-up находки discovery: provision БД шлёт `database create` (ед. ч.), а FastPanel 2 знает только `databases create` (мн. ч.) — БД, вероятно, не создаётся. Мутация вне read-only task9, нужна живая приёмка. Форма команд — `docs/FASTPANEL_CLI.md` §3 |
| `2026-08-13-domains-full-setup.md` | **выполнен целиком** (фазы 1–3) | Кейс 2 из `task8.md`: `POST /domains/full-setup` (бэкенд только персистит связки), Tauri-команда `domain_full_setup` (зона + write-back + NS), мастер «Add Domain» и массовое «Full setup». **Идемпотентности через idempotency key нет** — списывать было не с чего: у provision ключ живёт в десктопном SQLite, на бэкенде его не существует вовсе; сделана конструкцией. Ревью вскрыло: одиночный вход не работал (дубль имени → 500, имя не валидировалось вовсе, а `domain_name` уникален **глобально**, поэтому 500 прилетал и на чужом домене); поиск зоны по имени не фильтровался по аккаунту, из-за чего повторный full-setup мог записать домену `zone_id` **чужого** аккаунта и отбить регистратору nameservers чужой зоны при зелёном отчёте; отказ `POST /domains` не показывался нигде, хотя фаза 1 переписала роут именно ради этих кодов. Попутно закрыта дыра у соседей: `bulk-assign-server`/`bulk-assign-cloudflare` не проверяли владение целью. Живой долг — в итоговом блоке; **первым пунктом ручной приёмки** — живой прогон против настоящих Cloudflare/Hostiq/Namecheap: ни один агент к ним доступа не имел |
| `2026-08-17-hostiq-di-api-v3-i-rdap.md` | **выполнен целиком** (фазы 0–5) | Смена NS у Hostiq не работала НИКОГДА: клиент был выдуман — бил в `https://hostiq.ua/api` с `Authorization: Bearer`, хоста нет (`/api/domains` → 404 HTML), эндпоинта `PUT /domains/{d}/nameservers` не существует, а `registrarCaps.ts` рисовал живую кнопку. Настоящий API — DI-API v3 (`https://hostiq.ua/clients/di-api/v3`, один заголовок `X-Authorization-Token`; email для авторизации не нужен вовсе). **Чтения NS у API Hostiq нет в принципе** — поля нет ни у одного из 127 доменов, поэтому источником «как есть» для ВСЕХ провайдеров стал RDAP (`desktop/src-tauri/src/rdap.rs`), а чтение через API регистратора удалено целиком — трейт-метод, команда, парсер, хук. Бонус: ручной провайдер (Dynadot) впервые получил настоящий бейдж делегирования. **Три дыры, зелёные на всех тестах и найденные ревью:** (1) RDAP не работал ни на одном домене — `reqwest` не ставит `User-Agent`, Cloudflare перед `rdap.org` режет безUA-трафик, а живые проверки снимались `curl`'ом, то есть обоснование кода получено ДРУГИМ инструментом; (2) успех смены NS выводился из одного HTTP-статуса, и 200-страница прокси давала `ns_status = "ok"` + фальшивую строку `registrar.ns_set` в audit log; (3) пропавший `total` молча усекал листинг первой страницей, а `total` при нуле распознанных записей давал ложное «домена нет в аккаунте». Метод, который их поймал — **мутационная проверка гейтов**: три дыры (фолбэк на `count`, ветка таймаута RDAP, возможность вернуть `path_segments_mut` к `format!`) нашлись только снятием гейта. Контракт и пять молчаливых ловушек — `docs/HOSTIQ_API.md`; `dnspython` из requirements удалён (не импортировался ни строчкой), ложное «NS resolver fallback» из `docs/TECH_STACK.md` снято. Сюита 1019/95, `tsc` чист, 294 rust-теста + 1 `#[ignore]` (живая проба в сеть, утверждает по-настоящему — снятие `User-Agent` её роняет; изначально она печатала результаты без единого `assert`, то есть роль замка ей приписывалась зря, — поймало финальное ревью). **Осталась живая приёмка человеком** (6 пунктов, ожидается 127 доменов / 120 `Active`) и 7 пунктов долга в итоговом блоке; ценнее прочего — старые аккаунты Hostiq хранят нерабочий ключ без всякой подсветки в UI (миграции быть не может, блоб зашифрован на клиенте), отсутствие троттлинга RDAP на списке доменов и непроверенный живьём многостраничный обход. Исполнялся в одном дереве параллельно с чужой сессией — см. последний раздел плана |
| `2026-08-16-registrars-vybor-provajdera.md` | **выполнен целиком** (фазы 1–7) | Settings → Registrars: провайдер выбирается комбобоксом с поиском (API-каталог ∪ ранее использованные) либо создаётся вводом имени; поля секретов — только у провайдеров с рабочим Rust-клиентом (`hasApi`). Бэкенд/Rust не трогаем (`provider` уже свободная строка, блобы опциональны). Каталог `lib/registrarProviders.ts` — источник правды о **показе** провайдера, а способность к API **наследует** у существующего `lib/registrarCaps.ts`: свой предикат с `trim` дал бы строке `" hostiq "` бейдж API и живую кнопку Test в Settings при выключенном «Set NS» на карточке домена и `unknown provider` от десктопа за кнопкой. Согласие каталога с десктопным списком закрыто тестом. В правке аккаунта провайдер read-only (сменить его значило бы оставить блобы ключа привязанными к учётке другого регистратора), у ручного — только имя. Фаза 7 закрыла долг: `providerMeta`/`hasApi`/`needsClientIp` принимают `null` сами (страховок `String(provider || "")` на местах вызова было три, а `providerMeta(null)` **бросала** внутри `map` без error boundary — одна порченая строка уносила бы всю вкладку; колонка при этом НЕ nullable, терпимость — про битый ответ, а не про штатный `null`); провайдер-мусор (`" hostiq "`) больше не выглядит неузнанным Hostiq — сырая строка показывается в кавычках на карточке и в правке, `label` остаётся чистым для списка и дедупа; комбобокс возвращает фокус на кнопку после выбора; подсказки поля «API User» уехали в каталог (`apiUserField`) — выбирать их тернарником по `needsClientIp` значило использовать «нужен ли Client IP» прокси для «это Namecheap». Сюита 988/94, `tsc` чист. Финальное ревью ветки поймало блокер слияния: симлинк `frontend/node_modules` был закоммичен (правило `.gitignore` со слэшем — шаблон каталога, симлинк под него не попадал) — снят, правило переписано. **Осталась живая приёмка человеком** (три пункта с готовым лечением в разделе «Живая приёмка» Фазы 7): подрезается ли раскрытая выпадашка прокруткой модалки (худший случай — ручной провайдер, под комбобоксом нет ни одного поля), два одинаково зелёных чипа `Active`/`API` в строке, кегль буквы в аватаре выпадашки (11.2px после вывода `size × 0.4`). Известные ограничения — в итоговом блоке: провайдер-мусор из UI не чинится (только БД или пересоздание аккаунта). 7 фаз, чисто фронт |
| `2026-08-17-kartochka-domena-svyazi-i-registrar.md` | **выполнен целиком** (фазы 1–5) | Карточка домена отвечала на «что с сайтом» дважды: верхний блок — наша запись из provision, секция `Server state` — живое чтение по SSH, и пересекались они восемью полями; путь сайта печатался трижды. Стало: факт с сервера — единственный источник, наша запись всплывает строкой «при развёртывании: X» только при расхождении (`lib/domainDrift.ts`, три исхода `agree`/`drift`/`recorded-only`; `SSL_BADGE` переехала в `lib/domainFacts.ts`, чтобы сводка и секция считались одной функцией). Верх пересобран под цепочку связей Registrar → Cloudflare → Server; `Registrar` перестал быть сырым `registrar_id` и получил селект назначения — это сняло тупик «Assign a registrar account to this domain first» у панели NS. **Ревью вскрыло пять дыр, зелёных на всех тестах:** (1) пустой список фактов давал бы «при развёртывании: X» на КАЖДОМ домене — провод не отличает «баз нет» от «не прочитали» (`list_site_databases` схлопывает в `Ok(vec![])` и успех, и падение обеих команд), причём у FTP та же неоднозначность была решена в обратную сторону; (2) `digits()` давал ложное расхождение на patch-версии PHP (`7.4` vs `7.4.33`), хотя `coerce_php_version` три компонента прямо допускает; (3) правило FTP-логина стояло на несуществующем основании «первый = основной» — `filter_ftp_by_domain` лишь сохраняет порядок вывода CLI; (4) шапка модалки утверждала, что паролей на карточке нет — фраза пережила снятие блока, где «здесь» значило «в этом блоке», при живом `FtpPassword` с `RevealSecret`; (5) **`ssl_status` пропал бы молча** — план предписывал удалить поле `SSL`, но его пара на сервере это `sslState`, другая величина, а список красит домен по `ssl_status` до сих пор; хуже того, write-back провижининга при провале SSL **гасит `last_provision_error` в `null`** и текст certbot не сохраняет, так что от провала не оставалось ни бейджа, ни строки ошибки. Плюс горизонтальный скролл: `<select>` тянется по самой широкой опции при плашке ~200px, а сетка `Server state` осталась `1fr 1fr` (то есть `minmax(auto, 1fr)`). Замок проверен **мутациями** — девять сломанных правил, все девять убиты сюитой. Сюита 1122/99, `tsc` чист, `npm run build` проходит; бэкенд и Rust не тронуты ни строкой. **Осталась живая приёмка человеком** (6 пунктов; jsdom вёрстку не считает, поэтому ни один тест не проверяет геометрию — главный пункт: на ЗДОРОВОМ домене строк «при развёртывании» не должно быть ни одной) и 9 пунктов долга в итоговом блоке; ценнее прочего — колонка SSL в списке всё ещё считает по `ssl_status`, а `DomainCloudflareField` ведёт себя хуже нового соседа на двух состояниях. 5 фаз, чисто фронт |

> ⚠️ **Главное из Спринта 3 — закрыто фазами 1–2 Спринта 4.** Финальное ревью вскрыло, что
> **пути записи блоба секрета нет нигде**: миграция 011 дропнула плейнтекст-колонки без
> конвертации, `vault_put_blob` не имел вызывающих, а формы слали пароли в поля, которых
> нет в схемах (Pydantic по умолчанию `extra="ignore"` → 200 OK и ничего не сохранено), —
> и **ни одна из 26 Tauri-команд не могла отработать**. Сегодня все пять секретов уходят
> блобами (фаза 1), а незнакомое поле в теле — это 422 `extra_forbidden`, а не тишина
> (фаза 2, `extra="forbid"` на шести схемах записи). Детали и остаток —
> `2026-08-04-sprint4-secret-write-path.md`.

> ✅ **Долг Спринта 3 закрыт полностью.** Пункты №7–8 закрылись попутно в Спринте 4, №9–12 —
> cleanup-спринтом (`2026-08-04-cleanup-debt-9-12.md`). Главное из него: долг №10 оказался не
> про валидацию, а про **три двери** — пароль из URL панели уезжал в аудит-метаданные, в
> `servers.fastpanel_url` и эхом в `input` ответа 422; ревью нашло внутри самой фазы две
> настоящие дыры (обрезка control-хвоста шла до среза userinfo; `@` в пути пропускал
> `https://admin:12/345@ip:8888` через все три реализации). Долг №12 ценен не удалением двух
> схем, а гардом, который ловит `SecretStr` и схемы, объявленные прямо в роутах.
> **Незакрытые соседние долги** (мёртвый фронт на 10 несуществующих роутов, плейнтекст
> `"Webhook Secret"` в `system_config`, доки про несуществующие эндпоинты) — в итоговом блоке плана.

Следующий крупный кусок — Stage 5 (launch prep): не начат, артефактов нет
(`.github/workflows/`, `docker-compose.prod.yml`, `frontend/e2e/` отсутствуют).
План деплоя в `stage5.md` собран под Hetzner + Caddy + self-hosted Postgres, а
фактический стек — Supabase; **до исполнения его надо пересобрать**.

## Правила

1. **Один план = одна функция.** Если план для функции уже есть — работаем с ним, а не создаём новый.
2. **Имя файла:** `YYYY-MM-DD-название-функции.md` (например, `2026-08-05-billing-phase3.md`).
3. **Каждый план делится на фазы.** У каждой фазы статус `[ ]` (не сделано) или `[x]` (сделано).
4. **В конце плана — итоговый блок:** реализован целиком или нет, что осталось.
5. **Актуализация обязательна:** любой агент, работающий с планом, отмечает сделанные фазы
   и дописывает, что сделал, после каждой сессии.

## Цикл работы с планом

1. Claude пишет первичный план в `plans/YYYY-MM-DD-название.md`.
2. Ты копируешь ссылку на файл, открываешь **новый чат**.
3. В новом чате просишь глубокую критику плана через параллельных субагентов
   (техническая реализуемость, соответствие бизнес-целям, UX).
4. Возвращаешься к плану и обновляешь его с учётом критики.
5. Только после этого — реализация (по триггер-фразе «Реализуй фазу N по плану»).

См. шаблон одного плана: `_TEMPLATE-плана.md`.
