# План: Domains — full setup нового домена (Кейс 2)

**Дата:** 2026-08-13
**Источник:** `task8.md` (корень), Кейс 2. Первой половине — Кейсу 1 — отведён отдельный
план `2026-08-13-domains-cloudflare-zhivoy-match.md`.
**Ветка:** `feat/domains-cloudflare-match`

## Context (зачем)

Цель: из мастера завести домен и одним потоком связать хостинг + Cloudflare (+регистратор),
завести зону и (по тумблеру) прописать NS. Схемы `BulkFullSetupRequest/Response`
(`backend/app/schemas/domain.py`) уже есть, но **эндпоинт и десктоп-команда не реализованы**.

### Граница из-за zero-knowledge (важно)

- **Бэкенд** атомарно делает то, что не требует токена: создаёт строку домена и проставляет
  `server_id` + `cloudflare_account_id` + `registrar_id`; возвращает домен и `task_log_id`.
  Идемпотентность — через idempotency key (как у provision).
- **Десктоп** выполняет то, что требует токена: `cf_create_zone` → write-back
  `cloudflare_zone_id`; по тумблеру `registrar_set_nameservers` (write-back `ns_status`).
  Всё пишет audit best-effort, как остальные команды.

## Acceptance criteria

- [x] `POST /domains/full-setup` принимает `BulkFullSetupRequest` и проставляет связки пачке
      существующих доменов одной транзакцией, возвращая `BulkFullSetupResponse`.
- [x] Повтор не делает работу дважды. **Переформулировано по факту:** idempotency key нет —
      см. «Что изменилось против плана».
- [x] Tauri-команда `domain_full_setup` на ОДИН домен: заводит зону (или переиспользует
      существующую), пишет `cloudflare_zone_id` обратно, по флагу пушит NS.
- [x] Наружу команда отдаёт структуру со статусами шагов — частичный успех не крашится.
- [x] Мастер «Add Domain» умеет полный поток: домен → связки → зона → (по тумблеру) NS,
      с отчётом по шагам.
- [x] Массовое действие «Full setup» по выделенным: диалог → бэкенд → прогон по пачке с
      конкуренцией → сводный отчёт, гейт «один прогон за раз».
- [x] Тумблер «Прописать NS» дизейблится для регистратора без API; кнопка «Создать и
      настроить» при этом остаётся активной.
- [x] Бэкенд не ходит в Cloudflare и к регистратору — ни строчкой.

## Edge cases

- Зона уже существует в аккаунте → не дублировать (fallback на поиск по имени).
- Регистратор не выбран или его провайдер без NS-API → NS-шаг пропускается, остальное идёт.
- Частичный провал в пачке: отчёт строками, а не исключением.
- Веб (`!isTauri()`): шаги выполнения (зона/NS) недоступны — только связки, либо действие
  скрыто.
- Двойной клик по массовому действию — гейт, как у привязки/provision.

## Фазы

### Фаза 1 — Бэкенд: эндпоинт `[x]`

Поддержать **оба входа**:
- **Новый домен (одиночный).** Мастер сперва создаёт домен существующим `POST ""`
  (реюз `create_domain`), получая `id`, затем идёт в full-setup по этому id.
- **Массовый по существующим.** `POST /domains/full-setup` на базе готовой
  `BulkFullSetupRequest` (`domain_ids`, `server_id`, `cloudflare_account_id`,
  `registrar_id?`) — реализовать роут (сейчас только схема). Сервис `domain_service.py`:
  транзакция «проставить связки пачке» (реюз `bulk_assign_server`/`bulk_assign_cloudflare`),
  вернуть `BulkFullSetupResponse` (id для последующего write-back зоны из десктопа).

Бэкенд только персистит связки; зону/NS он не трогает (zero-knowledge).

### Фаза 2 — Десктоп: команда исполнения `[x]`

Новый `commands/full_setup.rs` — Tauri-команда `domain_full_setup` (на ОДИН домен): по
`domain_id`+`cloudflare_account_id` зовёт `cf_create_zone` (если зоны нет), пишет
`cloudflare_zone_id` обратно (`domain_write_back`), по флагу `push_ns` —
`registrar_set_nameservers`. Реюз `cloudflare::client::create_zone` (+ поиск зоны по имени
через `list_zones`, отдельной `get_zone_by_name` в клиенте сегодня нет) и трейта
регистратора; наружу — структура со статусами шагов (зона / NS / что не удалось).
Массовость — на фронте: пачка гоняет ту же команду с конкуренцией (паттерн `mapWithLimit`
из `cfAutoBind.ts` / гейт как у `provision_bulk`). Зарегистрировать в `lib.rs`.

### Фаза 3 — Фронт: мастер + массовое действие `[x]`

**Одиночный** (мастер из «Add Domain»): расширить `AddDomainModal.tsx` (или новый
`FullSetupModal.tsx`): поля Домен, Регистратор, Хостинг(сервер), Cloudflare-аккаунт +
тумблеры «Создать зону в Cloudflare», «Прописать NS у регистратора». Поток: `POST ""`
(создать домен) → (desktop) `domain_full_setup` → отчёт по шагам (зона создана / NS
прописаны / что не удалось), тон как у привязки.

**Массовый** (по выделенным): кнопка «Full setup» в `BulkActionToolbar.tsx` → диалог
(сервер + CF-аккаунт + регистратор + те же тумблеры) → `POST /domains/full-setup` →
(desktop) прогон `domain_full_setup` по пачке с конкуренцией → сводный отчёт (как
`CloudflareBindBanner`). Гейт «один прогон за раз», как у привязки/provision.

Desktop-only для шагов выполнения (зона/NS).

Уточнения UX (решения пользователя):
- **Счётчик доменов в выпадашках.** Сервер и CF-аккаунт показывать как «имя — N доменов»,
  наименее загруженные сверху (сортировка по возрастанию N). N — из локального списка
  `useDomains()` группировкой по `server_id` / `cloudflare_account_id` (считаем домены в
  SDMP, не живые зоны — быстро, работает и в вебе).
- **«Создать зону».** = `cf_create_zone` (POST /zones) — завести домен как зону в выбранном
  аккаунте, получить `zone_id` + NS; если зона уже есть, не дублировать. Инлайн-пояснение
  под тумблером.
- **Регистратор без API.** NS-API умеют только `hostiq` и `namecheap` (десктоп
  `make_service`, `registrars/mod.rs`). Хелпер `registrarSupportsNsApi(provider)` —
  общий с Кейсом 1 (`lib/registrarCaps.ts`, единственный источник правды, зеркалит
  десктоп): если провайдер не из этих двух или регистратор не выбран → **дизейблить только
  тумблер** «Прописать NS» + подсказка «нет API — пропишите NS вручную». Кнопка «Создать и
  настроить» остаётся активной.

## Переиспользуем (не плодим нового)

- Бэкенд: `create_domain`, `bulk_assign_server`, `bulk_assign_cloudflare` — `domain_service.py`
- Десктоп: `cloudflare::client::create_zone` / `list_zones`, `registrar_set_nameservers`,
  `domain_write_back`
- Фронт: `useCreateDomain` — `api/domains.ts`; `mapWithLimit` — `api/cfAutoBind.ts`;
  `registrarSupportsNsApi` — `lib/registrarCaps.ts` (из Кейса 1)

## Гарды корректности

- Zero-knowledge: бэкенд не ходит в Cloudflare/регистратор; зоны/NS — только десктоп.
- Full-setup идемпотентен (конструкцией, не ключом); частичный успех — строки отчёта, не краш.
- Существующая зона не дублируется.
- Гейт «один прогон за раз» на массовом действии.

## Верификация

1. Мастер нового домена → `full-setup` создаёт домен+связки → десктоп заводит зону,
   (по тумблеру) NS → отчёт по шагам; повтор идемпотентен.
2. Регистратор без NS-API → тумблер NS дизейблен, домен и зона всё равно созданы.
3. Массовое действие по выделенным → сводный отчёт, частичные провалы строками.
4. Тесты точечно: бэкенд-тест эндпоинта full-setup; фронт — диалог и сборка отчёта.

## Итог

- Реализован **целиком** (фазы 1–3), сессия 2026-08-13.
- Коммиты: Фаза 1 — `3690054`, `a3d654f`, `0b374b5`; Фаза 2 — `9b42c88`, `47f89d2`;
  Фаза 3 — `d540734` + правки по ревью.
- Живой долг — в разделах «Долг Фазы N» ниже.

### Что изменилось против плана

**Идемпотентность через idempotency key не сделана** (Фаза 1) — списывать было не с чего,
подробности ниже. Критерий «повтор с тем же ключом не делает работу дважды» закрыт другим
способом и потому переформулирован по факту.

**Мастер не ходит в `full-setup` вторым запросом.** План предполагал `POST ""` → затем
full-setup по полученному id; на деле связки уезжают тем же `POST /domains` (`DomainBase`
их принимает, а роут проверяет владение всеми тремя). Второй круг был бы лишним.

**`get_zone_by_name` в клиенте всё-таки была** — план утверждал обратное.

### Фаза 1 — что вышло по факту

Роут `POST /domains/full-setup` одной транзакцией проставляет связки пачке и отдаёт
десктопу `{id, domain_name}` — вход для создания зоны. В Cloudflare и к регистратору не
ходит ни строчкой (проверено буквально: `httpx` в `backend/app` живёт только в
`auth/email.py` и двух провайдерах уведомлений).

**Отступление от буквы плана: идемпотентности через idempotency key нет.** Требование
списывалось с provision, но на бэкенде у provision ключа **не существует вовсе** —
`grep -i idempot` по `backend/` даёт ноль попаданий; ключ живёт в десктопном SQLite
(`bulk_runs`, sha256 от action + отсортированных id), сервер о нём не знает, и роутов
provision на бэкенде нет в принципе. Копировать было нечего, а журнал ключей означал бы
новую таблицу и миграцию ради присваивания трёх колонок. Вместо этого идемпотентность
сделана конструкцией: строки, уже стоящие в целевом состоянии, выпадают из UPDATE, поэтому
повтор не двигает ни `sync_version`, ни `updated_at`. По существу это сильнее ключа —
держится и при разных ключах, и при разных клиентах, потому что операция есть чистое
присваивание. Слабое место — конкурентные запросы с **разными** целями: последний
побеждает, обоим сказано «связано» (та же семантика, что у `bulk-assign-*`).

**Схема ответа переписана.** `BulkFullSetupResponse` объявлял `task_ids` + `task_log_ids` —
контракт из мира, где выполнение жило на бэкенде. Потребителей у неё не было (на фронте
типы уже удалены), а отдавать `task_ids` значило бы врать: задач бэкенд не запускает.

**Частичный успех — `skipped_ids`, а не отказ пачки:** список приходит из выделения на
экране, и домен, удалённый в соседней вкладке, не повод не связать остальные 199. «Нет у
тебя» и «нет вообще» неотличимы намеренно — иначе роут перечислял бы чужое.

**Главное, что вскрыло ревью: одиночный вход не работал.** План назначил одиночным входом
существующий `POST /domains`, и тем самым втянул в объём два его старых дефекта. Дубль
имени давал **500** (`IntegrityError` не перехватывался нигде в приложении), а имя не
валидировалось вовсе — `{"domain_name": "не домен вовсе"}` возвращало 201, и мусор доехал
бы до десктопа как имя будущей зоны Cloudflare, то есть ошибка всплыла бы на шаге, где её
никто не ждёт, и **после** записи связок. Падал при этом не край, а мейнстрим: пользователь
открывает мастер и вводит домен, который у него уже заведён.

Вдобавок выяснилось, что `domains.domain_name` уникален **глобально, а не по
пользователю**, поэтому 500 прилетал и на чужом домене, а предпроверка занятых имён в
`bulk_create`/`bulk_create_structured` была сужена по `user_id` — чужое имя пролетало мимо
неё в `IntegrityError` на общем коммите, и **терялась вся пачка**.

Починено: валидация формы и длины → 422; конфликт → 409 (своё имя — предпроверкой, чужое и
гонка двух вкладок — перехватом `IntegrityError` с `rollback()`, разбор по `sqlstate`, а не
по тексту: текст зависит от локали сервера БД); занятое имя в пачке штатно уходит в
`skipped`. Ветка «не UNIQUE» пробрасывается как была — выдавать её за конфликт имени значило
бы врать.

Заодно закрыта дыра у соседей: `bulk-assign-server` и `bulk-assign-cloudflare` не проверяли
владение целью, то есть свои домены можно было привязать к чужому серверу по угаданному id.

### Долг Фазы 1

- **Уникальность `domain_name` глобальная.** Пока это так, факт «имя занято» наблюдаем для
  чужой строки в принципе — 409 сообщает ровно тот же бит, что прежний 500, только без вида
  поломки. Настоящее лечение — индекс по `(user_id, domain_name)` плюс решение, что делать
  с уже существующими коллизиями: миграция, отдельная задача.
- **`domain_ids` без верхней границы** (как и у соседних bulk-роутов): 100k id дадут
  гигантский `IN`. Лимит не поставлен, чтобы фронт не словил его неожиданно на «выделить всё».
- **Аудит на повторе пишется** даже когда ни одна строка не изменилась — это правильно (он
  фиксирует попытку), но означает, что «повтор не пишет ничего» верно только про домены.
- **Тестовые хелперы дублируются** между файлами тестов (в репозитории таких копий уже
  девять); общего `conftest`-кита нет.

### Фаза 2 — что вышло по факту

`commands/full_setup.rs` — команда на ОДИН домен, массовость на фронте. Наружу — отчёт по
шагам: `zone` (`created`/`existed`/`failed`), `zone_saved: bool|null`, `ns`
(`pushed`/`skipped` с пятью причинами/`failed`).

**Контракт ошибок разведён по типу, а не по флагу.** `Err` из команды означает «работа не
начиналась и по этому домену повтор бесполезен, пока не починено общее» — запертый keychain,
неинициализированная синхронизация, пустой ввод. Всё остальное приходит `Ok`-отчётом.
Выбран `Err`, а не поле `fatal: bool`, потому что флаг обязывал бы фронт помнить, что часть
`Ok` на самом деле значит «стой», — ту же развилку, только необязательную к соблюдению.
Реализовано `preflight` перед первым сетевым запросом плюс перехват гонки «заперли между
проверкой и вызовом».

**Главное, что вскрыло ревью: поиск зоны по имени не фильтровался по аккаунту.** Токен со
скоупом «All accounts» видит несколько аккаунтов CF. Если домен уже заведён зоной в аккаунте
B, `POST /zones` с `account.id = A` отвечает 1061, и поиск по имени находил зону **B**:
отчёт говорил `existed` зелёным, в `cloudflare_zone_id` уезжал id чужой зоны, а при
`push_ns` регистратору отбивались nameservers **чужой** зоны — то есть делегирование
уходило в аккаунт, о котором пользователь не просил. Это ровно тот исход, ради недопущения
которого `lib/cfZoneMatch.ts` во фронте отказывается угадывать при неоднозначности, — только
здесь он достигался вообще без угадывания. Второе лицо той же ошибки: повторный full-setup с
намерением **перенести** домен в другой аккаунт возвращал зону старого и рапортовал успехом.
Закрыто фильтром `account.id` в запросе плюс сверкой `account.id` в ответе (зона, про аккаунт
которой ответ молчит, при названном аккаунте считается неподтверждённой — незнание в сторону
«наша» не округляем).

**Попутно у Namecheap починен `get_nameservers`** — см. долг Кейса 1: он звал `getInfo`, а
разбирал элемент из ответа `dns.getList`.

`busy_timeout` кэша выставлен явно: премиса «мгновенный `database is locked`» оказалась
неверной (rusqlite сам ставит 5с на каждом открытии), но умолчание зависимости не видно по
коду и может уехать с версией, а пачка гоняется рядом с фоновой синхронизацией.

### Долг Фазы 2

- **Живого прогона против настоящих Cloudflare/Hostiq/Namecheap не было** — всё на wiremock.
- **«cloudflare account not in local cache» остаётся строкой отчёта**, хотя условие тоже
  общее для пачки; отличить от отказа Cloudflare можно только по тексту.
- **Ретрая на `zone_saved: false` нет** — зона в Cloudflare есть, SDMP о ней не знает;
  допишет следующий прогон.
- **Повторный прогон по уже настроенному домену тратит два вызова CF** — команда не знает
  про уже записанный `cloudflare_zone_id`.
- **`preflight` и `save_zone_id` целиком** требуют `AppHandle`/`State` и покрыты только через
  извлечённые из них чистые куски.

### Фаза 3 — что вышло по факту

Мастер «Add Domain» расширен общими полями и двумя тумблерами; массовое «Full setup» —
кнопкой в тулбаре и диалогом. Чистые правила (дизейбл тумблеров, сортировка выпадашек по
загрузке, план прогона) — в `lib/fullSetupPlan.ts`; прогон и сводка — в `api/fullSetup.ts`.

**Гейт «один прогон за раз» вынесен в `api/runGate.ts`** и переиспользован, а не скопирован;
`useCloudflareBind` переведён на него. Мастер намеренно **не** гейтится — иначе домен,
заведённый во время идущей пачки, молча остался бы без зоны (та же причина, по которой не
гейтится автопривязка).

**Остановка пачки на `Err`** сделана списком `stopped` с проверкой внутри воркера: уже
начатые договаривают, новые не стартуют, в отчёт уходит `aborted {domain, error, notSetUp}`,
где `notSetUp` считает и того, на ком встали, и тех, до кого не дошли. `zone_saved: false`
поднимается в сводку отдельным предложением с тем, что делать, — на сотне доменов тост
утонет, а построчный отчёт никто не читает.

`CloudflareBindBanner` обобщён в `RunNoticeBanner`: второй баннер отличался бы только
`aria-label`.

**Умолчания тумблеров асимметричны осознанно:** в мастере оба выключены (при нетронутых
тумблерах он ведёт себя ровно как прежний «Add Domain»), в массовом диалоге зона включена
(иначе «Full setup» дублировал бы «Assign Server/CF»). NS выключены в обоих местах — это
смена делегирования живого трафика.

**Ревью нашло два пробела:** отказ `POST /domains` (409/422/404) не показывался нигде —
модалка оставалась открытой молча, хотя Фаза 1 переписывала роут именно ради этих кодов; и
гейт полной настройки не был проверен ничем — снятие `runExclusive` не роняло ни одного из
22 тестов, а вместе с гейтом не проверялся и признак `pending` (то есть незамеченными
остались бы негаснущая кнопка и второй прогон поверх первого — вторая смена делегирования у
регистратора, а не идемпотентный повтор).

### Долг Фазы 3

- **Язык смешанный:** диалог и тумблеры по-русски, отчёт по-английски рядом с отчётом
  привязки. Общий долг ветки, решается разом с языком вкладки.
- **Конкуренция прогона (2) подобрана рассуждением, не замером** — цена ошибки время, не
  данные.
- **Отчёт не разделяет `zone.failed` по причине:** 1061 «имя занято вне видимости токена»
  показывается текстом первого провала, без своей ветки.
- **`Domains.tsx` держит состояние пяти диалогов подряд** (520 строк против 423) — следующую
  функцию с диалогом заводить уже с вынесенным кластером, иначе разбивка отыграется назад.
